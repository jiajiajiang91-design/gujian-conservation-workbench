# 13-13 · 当前实现独立审查报告 v0.1

## 1. 审查结论

审查日期：2026-08-13。

当前仓库已经形成产品文档、旧工作台、领域内核、项目包原型、三维几何试验和跨格式制图试验，但尚未连成可验证的专业成果生产系统。旧工作台可以运行；v2 制图和 v3 几何有可复用代码；当前不存在已经完成的专业成功样本。

三维和图纸成果统一保持以下状态：

```text
generated-not-qualified
L1=false
```

这些文件可以用于内部技术验证，不能作为正式测绘、施工、保护设计或专业签发成果。

## 2. 审查范围

本次检查了：

- 核心 PRD、技术架构、专业制图质量基准和项目上下文；
- 旧 React 工作台、服务端模型路由、项目包和代理成果逻辑；
- `packages/domain`、`packages/application` 和未完成的基础设施包；
- `workers/cad/t0b_v2` 的字体、Drawing IR、DXF、SVG、PDF 和检查记录；
- `workers/cad/t0b_v3` 的合同、语义几何、GLB、来源网格和验证记录；
- 本地 HABS 子集、高都和东呈项目数据；
- Git 分支、远端、未提交改动、忽略规则和可能进入公开仓库的文件。

## 3. 当前状态

| 范围 | 状态 | 审查判断 |
|---|---|---|
| 终局 PRD | 部分完成 | v1.0 方向正确；人工节点、同源成果、样本和资格门槛需要修订。 |
| 技术架构 | 部分完成 | v1.1 已定义目标架构，但实现尚未达到文档描述；PRD v1.1 确认后再修订。 |
| 旧 React 工作台 | 部分完成 | 回归测试、类型检查和构建通过；页面仍直接导入高都、东呈，并按固定 UUID 选择成果分支。 |
| 项目包 | 部分完成 | 已有 JSON、ZIP 和安全测试原型；尚未完成通用资料导入、完整资源闭包和持续更新演示。 |
| AI 助手 | 未完成 | 当前页面明确显示本地建议；部分任务仍路由到 `moonshot-v1-128k`，没有完整 Kimi K2.6 运行生命周期。 |
| 领域内核 | 部分完成 | 已有项目、事实、命令和来源基础；审计字段改动缺测试、迁移和完整持久化实现。 |
| 基础设施包 | 未完成 | 当前只有包配置和 TypeScript 配置，没有源码，不能视为已建立存储层。 |
| v2 字体闭包 | 已完成技术原型 | 官方字体、OFL、元数据、确定性派生和字体测试可保留。 |
| v2 Drawing IR 与跨格式生成 | 部分完成 | 真实生成 DXF、SVG、PDF 和 PNG；与当前 v3 几何不同源，专业审查未通过。 |
| v3 语义几何 | 部分完成 | 中性术语、稳定 ID、接口和攻击测试可保留；合同和验证证据仍有阻断问题。 |
| HABS 成功样本 | 未完成 | 本地 Dai Loy 只有官方记录的精简子集；Badin-Roque 尚未取得和运行。 |
| 中国阻断样本 | 可进入实施 | 高都和东呈均缺正式成果所需实测证据，可验证资料不足阻断。 |
| 正式身份与签发 | 未完成 | 当前只有数据字段和代理身份，不具备可验证权限和责任签发。 |

## 4. 已通过的检查

### 4.1 旧工作台

2026-08-13 重新执行：

- 旧服务与迁移回归：13 项通过；
- React、领域、命令、项目包和代理交付单元测试：17 项通过；
- TypeScript 检查通过；
- Vite 生产构建通过。

这些结果只证明旧入口仍可运行。固定项目 ID、内置样例分支和未调用模型等问题仍然存在。

### 4.2 CAD 合同和输出原型

- v3 合同测试：11 项通过；
- v2 合同、Drawing IR、DXF 和字体测试：45 项通过；
- 跨格式生成测试：7 项通过；
- 跨格式独立攻击测试：10 项通过。

测试能够证明部分文件格式、来源映射和确定性属性，不能替代古建专业审查。

## 5. 阻断问题

### 5.1 产品与数据

1. 旧界面在 `App.tsx` 直接导入两套样例，在 `Workspace.tsx` 按固定项目 UUID 选择制图输入。复制项目或第三项目不能复用同一流程。
2. 资料页仍以展示为主，没有完成任务书、照片、测量记录和历史图纸进入项目版本的完整流程。
3. 成果记录不能完整还原资料、候选、规则、人工决定、几何输入和成果之间的关系。
4. `packages/infrastructure` 没有源码；根依赖和锁文件也未形成可安装闭包。

### 5.2 模型调用

1. 当前模型注册表仍把部分任务路由到 `moonshot-v1-128k`。
2. 页面未进行真实调用时仍只能展示本地建议，尚无完整的排队、流式、重试、取消、迟到响应、用量和费用记录。
3. 现有环境变量只能保存在服务端；任何真实密钥不得进入项目包、浏览器或 Git 历史。

### 5.3 v3 几何

1. 当前 prefreeze 构建记录与 manifest、来源网格和 GLB 哈希闭合，但 `prefreeze-verification.json` 仍引用上一版文件，结论已经失效。
2. `geometry/` 是 881 个实体、24 个接口的旧结果；当前 prefreeze 是 886 个实体、2118 个接口。两套结果不得混用。
3. `longitudinalSegments`、`countPerLeaf`、`rows`、`columns` 等离散数量被记录成毫米。
4. 27 个构件模板都标为演示 L1 eligible，不能表达未知构造和专业审查阻断。
5. 关键接口主要验证距离接近，尚不能证明承托面、槽口面和搭接面的专业语义。
6. 验证器不能在技术检查通过后直接输出可冻结资格；专业判断必须来自独立审查记录。

### 5.4 v2 图纸

1. v2 图纸绑定旧 GeometryRevision，不是当前 v3 几何的同源成果。
2. 当前 DXF 变更后尚未重新完成独立双构建和 AutoCAD 审查。
3. 跨格式专业审查已拒绝当前图纸：图内出现英文内部 ID、4 处文字碰撞、固定构建时间被当作出图日期、现状和保护候选层为空、承托节点过度简化。
4. QCAD 可以打开和编辑文件，但保存会重建 33 个原生尺寸并解除 10 个视口锁定，不能称为无损往返，也不能回写规范成果。

### 5.5 样本

1. 本地 Dai Loy 只有 6 个文件，不能代表完整 HABS 成功路径。
2. Badin-Roque House 是下一套候选，取得完整资料并核对许可前不能宣称成功。
3. 高都和东呈缺少正式成果所需实测证据。两者只能输出资料清单、未知项、补测要求和交付阻断，不得用演示尺寸补成正式成果。
4. HABS 成功只能验证资料到成果的工程流程，不能证明系统已经掌握中国古建木构语义。

## 6. 未提交改动处置

| 改动 | 处置 |
|---|---|
| `.gitignore` 的 staging 规则 | 单独提交。 |
| 官方字体派生、manifest、脚本和测试 | 保留并单独提交。 |
| v2 字体绑定和跨格式代码 | 保留为旧链路工程原型；不得标为当前专业成果。 |
| v2 跨格式报告和必要审查截图 | 保留拒绝结论和哈希摘要；不提交大体积重复 SVG。 |
| 删除的 AutoCAD、QCAD 证据 | 记录为绑定旧 DXF 的失效证据，不静默删除。 |
| v3 合同、生成器和验证器 | 修复单位、资格、未知项和表面语义后拆分提交。 |
| v3 旧 `geometry/` 与预览 | 标记为 superseded，不作为当前输出。 |
| v3 prefreeze 输出 | 重生成独立报告前不提交为通过证据。 |
| `packages/infrastructure` 配置 | 补源码、测试和锁文件后再提交。 |
| 审计记录 schema 改动 | 补契约测试和迁移策略后再提交。 |
| `.projections.staging-*`、缓存和 `__pycache__` | 排除。 |
| `.env`、外部 DWG、商业软件临时配置 | 禁止提交。 |

## 7. GitHub 发布边界

远端仓库为 `jiajiajiang91-design/gujian-conservation-workbench`。现有公开版本已在本地标记为 `portfolio/v0.0`。本轮使用 `codex/professional-workbench-rebuild` 分支，不直接覆盖远端 `main`。

公开前必须完成：

- 全历史密钥、用户名、绝对路径和私有资料扫描；
- 样本、字体、截图和生成文件许可检查；
- 失效报告和专业未通过成果的状态校正；
- README 中高都成功路径等过期表述修订；
- 构建、测试和运行说明复核。

当前远端可继续作为临时求职链接，但不能把其中的高都代理成果或专业 CAD 描述为已经达到正式标准。
