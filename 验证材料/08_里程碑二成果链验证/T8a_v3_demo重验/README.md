# T8a v3 demo 保全与技术重验

本阶段保留上一轮 v3 的稳定构件 ID、几何实体、接口关系、GLB 和独立验证方法，并修正已确认的数据与几何问题。T8a 不证明通用化，也不授予专业资格。

## 修正内容

- 以 `length / angle / count / ratio / text` 区分参数，724 个计数参数使用无量纲单位 `1`，不再写成 mm。
- 将未知项改为结构化记录，保存原因、所需证据、受影响主张和资格阻断。
- 27 个构件模板的 `l1DemoEligibility` 均为 `blocked`。
- 每条接口绑定可独立复算的面片集合、面积、质心、法向和 patch hash。
- 将椽檩矩形假槽改为按圆檩实际截面生成的承托曲面；独立体积验证的非预期穿插从 185245.855989 mm³ 降为 0。
- 修正正脊与坡顶瓦作的收口，保留中央 demo 收口深度，并为瓦面建立受控承托面；独立体积验证为 0。
- 技术验证器只输出“demo 技术重验通过”，不再输出可冻结或专业合格结论。

## 当前结果

- GeometryRevision：`4579a720-0bff-5df7-b293-191483384059`
- 实体：886
- 接口：2118
- 结构化未知项：911
- GLB 网格：886，和 source mesh 的最大坐标误差 0.00047388 mm
- 561 条非瓦件接口最大非预期穿插：0 mm³
- 384 个屋面覆盖采样：384 个首个命中均为陶瓦构件
- 独立验证失败项：0

## 资格边界

结果保持 `generated-not-qualified / L1=false / formalEligibility=ineligible`。当前 fixture 是团队 demo，不是某座真实中国古建的测绘事实。真实类型、材料、现场尺寸和专业构造仍需证据与专业复核。T8b 仍需抽取无固定项目、柱网、瓦列和门窗数量的通用 GeometrySpec 内核；T9 仍需据新 revision 重生成同源视图与图纸。

大文件位于 `apps/server/.data/acceptance/milestone-two/t8a-demo`，不进入 Git 主线。Git 只保存源码、fixture、小型哈希清单和验证报告。
