# V1A 技术架构与开发任务

> 版本：V1.0  
> 状态：D1 至 D5 已实施并通过独立复核；V1A 正式成果验证未完成  
> 更新日期：2026-08-09  
> 上游产品定义：`13-8_核心PRD_V1A正式立面成果闭环.md`  
> 当前实施范围：项目数据独立化、双数据集、导入/切换/导出、基础校验

## 1. 架构目标

V1A 的技术目标不是一次性重写原型，而是在保留现有三栏工作台和可用交互的前提下，先解除“界面代码等于高都样例”的绑定，建立能承载不同项目、能校验、能恢复的数据基础。后续再逐步拆分编排器、业务服务和正式出图能力。

本轮完成后应满足：

- 项目数据存放在独立 JSON 中，界面代码不再保存整套高都业务数据。
- 一个项目一个数据包，具备版本、唯一 ID、资源映射和统一必填结构。
- 内置至少两个明显不同的项目，切换时不改代码。
- 每个项目的本地操作状态隔离保存。
- 用户能导入、选择、导出项目数据；无效数据不会覆盖当前项目。
- 示例、AI、程序、实测、人工、未知和缺失继续使用明确状态区分。
- 现有模型代理、状态机、规则检查和视图仍可运行。

## 2. 当前实现审计

### 2.1 改造前结构

原型采用原生 HTML/CSS/JavaScript 与 Node 服务：

```text
index.html
├─ js/data.js              高都项目完整数据与项目占位列表
├─ js/store.js             单一 localStorage 状态
├─ js/orchestrator.js      对话、流程、模型结果写入、部分业务逻辑
├─ js/checkrules.js        确定性检查
├─ js/drawgen.js           数据校核 SVG
├─ js/views/*              各工作阶段视图
└─ js/main.js              启动与全局渲染

server.mjs / api/* / server/*
└─ 模型代理、模型配置、调用记录、服务端测试
```

以上是本轮开始前的结构快照。本轮实施后，`js/data.js` 已改为项目仓库入口，业务数据移入 `data/projects/*.json`；`js/store.js` 已改为按项目 ID 隔离状态。完整结果见第 9 节和 `13-10_V1A数据基础开发验收记录.md`。

### 2.2 改造前已有基础

- 状态机控制主流程，模型只做意图与非标准输入处理。
- 规则检查独立于模型。
- 业务状态集中在 `Store`，视图主要通过方法修改状态。
- 来源状态已经区分 `demo`、`ai`、`program`、`measured`、`human`、`unknown`、`missing`。
- 前端不保存模型密钥，模型调用经过服务端代理。
- 视图切换清理局部订阅，避免重复渲染回调累积。

### 2.3 本轮要解决的结构问题

| 问题 | 当前表现 | 风险 |
|---|---|---|
| 项目数据与代码耦合 | `js/data.js` 内嵌整套高都数据 | 新项目需要改代码，无法证明通用性 |
| 项目列表是假切换 | 非高都项目只显示提示 | 双数据集验证无法执行 |
| 存档不隔离 | 所有数据使用同一 `gujian-wt-state` | 项目间相互覆盖 |
| 资源硬编码 | 多个视图固定 `gaodu_*.jpg/png` | 切换后仍显示高都内容 |
| 导入导出缺失 | 只能恢复单一示例 | 数据不可交接、不可恢复 |
| 编排器过大 | `orchestrator.js` 同时做路由、流程、业务写入 | 后续修改容易互相影响 |
| 字段契约不明确 | 浏览器直接信任全局对象 | 无效数据会在运行中才报错 |
| 正式图并非全数据驱动 | 正式图使用固定 PNG，校核图才实时生成 | 易让用户误以为正式图已随数据更新 |

## 3. 方案比较

### 方案 A：继续在 `data.js` 增加多个项目对象

优点是修改少；缺点是数据仍与代码绑定，导入、schema 迁移和项目级存档都难以建立。只适合一次性演示，不满足辅导 11 的数据独立要求。

### 方案 B：直接重写为完整前端框架与数据库

可以一次建立清晰分层，但会同时改变运行方式、状态管理、视图和服务接口，回归范围过大。当前尚未通过 V1A，不应先承担平台化成本。

### 方案 C：增加项目仓库层，渐进式替换硬编码（采用）

保留当前原生前端与 Node 服务，引入 `ProjectData` 作为项目数据唯一入口：启动时加载索引和默认 JSON，切换时激活对应数据包，`Store` 按项目 ID 保存可变状态，视图从资源映射读取图片。后续再把编排器中的业务逻辑逐步移到领域服务。

该方案满足本轮要求，改动集中，也不会提前锁定未来框架。

## 3.1 独立架构复核与修订结果

2026-08-09 由独立 solution architect 角色复核 PRD、架构和当前代码。复核最初给出 6 个阻断问题；本轮按下表完成修订。该复核通过的是 D1 至 D5 数据基础，不代表 V1A 已经取得正式成果。

| 阻断问题 | 修订结果 |
|---|---|
| 高都“实测缺失”与 `measured` 冲突 | 高都与东呈统一标为 `demo-proxy`；高都 5 项尺寸转写降为 `demo`，没有原始实测证据时不能正式交付 |
| 导入 JSON 可注入属性或远程资源 | `UI.esc` 增加引号转义；导入增加深层结构、长度、数量、ID、位置框和来源枚举校验；资源只允许 `assets/` 安全文件名 |
| 三开间、高都图片、比例与规范硬编码 | 资源、比例、规范、遮挡和项目文案改为数据读取；尺寸核对使用项目 `尺寸关系[]`，高都与东呈分别声明三开间和五开间关系 |
| 异步模型结果可能污染切换后的项目 | AI 请求未结束时禁止切换或导入，并向用户说明原因；任务度量按项目 ID 记录 |
| 静态正式图可能冒充当前结果 | 正式图资源增加项目 ID、生成版本、时间、参数摘要和 `preview/current/stale`；业务数据变化后自动失效 |
| 导出会覆盖原始证据 | 导出包分为 `原始证据`、`当前业务`、`工作流`、`审计`；导入时再标准化为运行数据，消息和临时界面状态不进入包 |

同时统一来源枚举为 `program`，只把旧 `rule` 作为迁移别名。旧单键存档采用“复制到项目键并保留旧键”的方式迁移。

## 4. 目标架构

```text
用户输入 / 本地项目 JSON
          │
          ▼
ProjectData 项目仓库
├─ 读取 index.json
├─ 校验 schemaVersion 与必填结构
├─ 加载内置项目
├─ 管理浏览器导入项目
└─ 导出当前项目数据包
          │
          ▼
Store 项目状态层
├─ 按 projectId 隔离 localStorage
├─ 合并项目基线与当前修改
├─ 记录操作、来源和责任状态
└─ 发出状态更新
          │
    ┌─────┴─────────────┐
    ▼                   ▼
确定性业务能力          AI 任务能力
├─ 状态机              ├─ 资料分类
├─ 尺寸与检查规则       ├─ 任务书提取
├─ 数据校核图           ├─ 草图转写
└─ 交付门槛             ├─ 视觉识别
                        ├─ 专业问答
                        └─ 意图路由
    │                   │
    └────────┬──────────┘
             ▼
三栏工作台视图
├─ 项目与阶段导航
├─ 结构化业务视图
├─ 证据和图纸视图
└─ 单一 AI 助手
             │
             ▼
项目 JSON / 图纸 / 检查与确认记录 / 交付清单
```

## 5. 模块职责与接口

### 5.1 `ProjectData` 项目仓库

**职责**：只处理项目包的发现、读取、验证、激活、导入和导出，不处理工作流程或页面渲染。

建议公开接口：

```js
await ProjectData.init()
ProjectData.list()
await ProjectData.load(projectId)
ProjectData.activate(bundle)
ProjectData.validate(bundle)
await ProjectData.importFile(file)
ProjectData.buildExport(currentState)
ProjectData.download(bundle)
```

错误约定：

- 读取失败：返回带项目 ID 的明确错误，不改变当前激活项目。
- schema 不支持：拒绝导入，说明支持的版本。
- ID 冲突：内置项目不可覆盖；导入项目要求用户改 ID 或由系统生成副本 ID。
- 资源缺失：数据仍可打开，但对应资料标记不可用，不能静默替换为另一个项目图片。

### 5.2 项目索引

`data/projects/index.json` 只保存项目发现信息，不复制业务内容：

```json
{
  "schemaVersion": "1.0",
  "defaultProjectId": "gaodu",
  "projects": [
    {
      "id": "gaodu",
      "name": "高都玉皇庙主殿",
      "url": "data/projects/gaodu.json",
      "builtin": true
    }
  ]
}
```

项目总览的成果、地点、比例、状态和待办可以放在索引摘要中，进入项目后以数据包为准。

### 5.3 项目数据包

数据包使用 UTF-8 JSON。顶层结构遵循核心 PRD 的最小数据模型，并增加资源映射：

```json
{
  "schemaVersion": "1.0",
  "项目": { "id": "gaodu", "名称": "...", "成果": "..." },
  "资源": {
    "主图": "assets/gaodu_main.jpg",
    "斜视图": "assets/gaodu_oblique.jpg",
    "正式图": {
      "path": "assets/gaodu_a3.png",
      "sourceProjectId": "gaodu",
      "generationVersion": "poc-2026-08-06",
      "generatedAt": "2026-08-06T00:00:00Z",
      "parametersHash": "sha256...",
      "status": "preview"
    },
    "样式参考图": "assets/gaodu_elevation.png"
  },
  "任务书原文": "...",
  "任务卡": {},
  "人员": [],
  "资料": [],
  "实测": [],
  "构件": [],
  "存疑": [],
  "现状": [],
  "图纸样式": {},
  "检查问题": [],
  "交付": {}
}
```

流程步骤属于产品定义，不属于单个项目，继续由共享代码维护。项目包不复制步骤，以免每个项目拥有不同状态机。

### 5.4 `Store` 状态层

存储键从单一键调整为：

```text
gujian-wt-state:<projectId>
```

规则：

- `fresh(bundle)` 只从当前项目包创建状态。
- `init(bundle, projectList)` 在其他视图初始化前执行。
- `switchProject(id)` 先确认没有未完成的模型请求，再加载和验证、保存当前项目并激活新项目。
- `reset()` 只删除当前项目的操作状态，不影响其他项目和导入项目清单。
- 旧的 `gujian-wt-state` 只允许一次迁移到 `gaodu`，迁移后不再写入。
- 导出包分离原始证据、当前业务、工作流与审计；排除消息、当前选中行和临时框选。
- 项目索引负责名称、地点、成果等发现信息；项目 Store 只覆盖当前步骤、待办和交付状态，数据包仍是业务内容基线。

### 5.5 资源读取

各视图统一通过 `DATA.资源` 获取项目资源：

- 构件核对：`资源.主图`
- 现状记录：`资源.斜视图 || 资源.主图`
- 正式图纸：`资源.正式图`
- 出图样式：`资源.样式参考图 || 资源.正式图`

若资源不存在，显示明确的空状态。禁止回退到高都图片，否则会造成证据错配。当前 JSON 不嵌入图片二进制，只保证结构化数据往返；完整资源包需要后续定义 ZIP 或 IndexedDB 方案。

### 5.6 模型服务边界

- 浏览器只发送完成当前模型任务所需的最小数据，不发送完整项目包。
- 模型响应必须经过现有 JSON/schema 校验后再写入 `Store`。
- 模型不直接写文件、不改流程状态、不生成正式交付资格。
- 密钥继续只存在服务端环境变量；`.env`、请求头和密钥不得进入导出 JSON。
- 记录任务类型、模型、耗时、成功与否和错误类别，不记录不必要的原始敏感内容。

## 6. 数据校验与迁移

### 6.1 V1 校验层级

**导入时硬校验**：

- 顶层必须是对象。
- `schemaVersion` 必须是当前支持版本。
- `项目.id`、`项目.名称` 必须为非空字符串；`项目.成果` 字段必须存在，范围尚未确认时可为空并停在业务检查。
- `资料`、`实测`、`构件`、`存疑`、`现状`、`检查问题` 必须为数组。
- `任务卡`、`图纸样式`、`交付`、`资源` 必须为对象。
- 构件编号、资料 ID 在各自集合中不得重复。

**打开后业务检查**：

- 缺少实测基准不拒绝打开，但交付状态降级。
- 资源不存在不拒绝打开，但证据项标记不可用。
- 未决问题不拒绝打开，但按影响范围阻止正式交付。
- `ai`、`demo`、`unknown`、`missing` 的关键字段不能被视为正式依据。

### 6.2 版本策略

- 当前 schema 版本为 `1.0`。
- 小版本只增加可选字段；大版本改变必填结构。
- 导入旧版本时只能通过显式迁移函数升级，不在视图中零散补字段。
- 不支持的新版本直接拒绝，并保留原文件不变。

## 7. 正式成果与校核图边界

当前原型有两种图：

- **正式图预览**：由既有参数化脚本产出的静态 PNG，用于证明专业表达方向。
- **数据校核图**：由当前构件与实测数据实时生成 SVG，用于证明数据驱动和修改反馈。

本轮把两者资源项目化，并实现预览版本与失效状态，不宣称静态正式图会随数据实时更新。界面明确标注“既有成果预览”与“当前数据校核图”；检查未运行、未决项未处理、正式图不是 `current` 或项目是代理数据时，不能进入正式交付。真正的数据驱动 CAD/DXF 生成属于后续独立任务，需要先定义参数契约、单位、图层和回归样本。

## 8. 可回退开发任务

每个任务都要求独立验收，后一个任务不能通过复制前一个任务的特例来通过。当前目录不是 Git 仓库，因此本轮没有独立提交历史；回退边界以文件清单、功能入口和自动测试记录为准。在 JIAJIA 确认是否建立版本库前，不声称具备提交级一键回退。

### D1 项目数据外置与 schema 基线（已完成）

**变更**：

- 把高都数据从 `js/data.js` 移到 `data/projects/gaodu.json`。
- 新建项目索引和数据校验函数。
- `js/data.js` 缩减为项目仓库加载器。

**独立验收**：删除浏览器存档并刷新，默认项目仍能完整加载；JSON 无效时显示启动错误，不以空对象继续。

**回退**：恢复原 `data.js` 和原脚本启动顺序，不影响服务端。

### D2 双数据集与项目规则解耦（已完成）

**变更**：

- 新增东呈古佛堂独立数据包。
- 将主图、斜视图、正式图和样式参考图改为项目资源映射。
- 将比例、规范、遮挡区域和尺寸关系改为项目数据；东呈使用五开间关系。
- 去除视图中的 `gaodu_*` 固定路径。

**独立验收**：两个项目的名称、构件、资料、图片和成果状态不同；东呈无实测基准，保持草图状态。

**回退**：从索引移除东呈项目即可，不影响高都。

### D3 项目级存档与真实切换（已完成）

**变更**：

- `Store` 增加初始化和项目切换接口。
- localStorage 按项目 ID 隔离。
- 项目总览从索引生成并支持真实切换。

**独立验收**：A 项目修改不会出现在 B；来回切换保留各自状态；当前项目渲染无旧项目残影。

**回退**：恢复单键存档，只影响浏览器本地状态，不改变项目 JSON。

### D4 导入与导出（已完成）

**变更**：

- 项目总览增加导入 JSON、导出当前项目入口。
- 导入执行硬校验并处理 ID 冲突。
- 导出分离原始证据、当前业务、工作流和结构化审计，排除密钥、消息与纯界面临时字段。

**独立验收**：导出文件可重新导入；错误 JSON、缺字段和重复 ID 均不覆盖当前项目；导出文件不含密钥。

**回退**：移除入口和导入项目索引，内置项目仍可使用。

### D5 数据自动测试（已完成）

**变更**：

- Node 测试读取索引和全部内置 JSON。
- 校验必填结构、唯一 ID、资源路径、来源状态、恶意字段、项目隔离、导出—导入和至少两个差异项目。
- 前端修改文件加入语法检查。

**独立验收**：`npm run check` 与 `npm test` 通过；破坏任一必填字段时测试失败。

**回退**：测试文件可独立移除，不影响运行。

### D6 编排器分层（后续，不在本轮）

把 `orchestrator.js` 分为意图路由、工作流服务、对象写入服务、问题处理服务和交付服务。先用当前行为测试固定输入输出，再逐模块迁移，禁止整体重写。

### D7 数据驱动正式 DXF（后续，不在本轮）

建立业务对象到参数化图纸的明确契约，服务端接收已确认参数，使用规则生成 DXF/PDF，规则检查读取同一结构化数据。需要高都与东呈两个回归样本，以及专业人员对图层、尺寸、表达和误差的检查。

### D8 PDF 与图纸资料解析（后续，不在本轮）

先定义可提取字段和证据回链方式，再接入 PDF 文本/图像解析。解析结果只作为建议，不直接进入正式依据。

## 9. 测试策略

### 9.1 自动测试

- schema：所有内置数据包必填结构、类型、ID 唯一。
- 项目差异：至少两个项目 ID、名称和构件集合不同。
- 来源状态：只允许规定枚举；东呈估算数据不得标为 `measured`。
- 资源：索引声明的数据文件和项目资源必须存在。
- 服务端：保留现有模型注册与代理测试。
- 语法：修改过的浏览器脚本使用 `node --check`。

### 9.2 浏览器冒烟测试

1. 启动服务并打开项目总览。
2. 打开高都，进入任务、构件和图纸页面。
3. 修改构件名称，切到东呈，再切回高都。
4. 检查修改隔离和项目图片。
5. 导出当前项目，重新导入为副本。
6. 尝试导入无效 JSON，确认当前项目不变。
7. 断开模型服务，确认浏览、人工修改、规则检查和导出仍可用。

### 9.3 专业验收

自动测试只能证明数据结构和产品行为，不能证明图纸专业正确。V1A 代理验收仍需按 `13-8` 的场景检查：构件身份、实测依据、表达规则、检查结论、成果性质和责任记录。

## 10. 安全、隐私与失败处理

- 禁止读取、导出或提交 `.env`。
- 导入 JSON 作为不可信数据处理；只解析 JSON，不执行其中任何脚本或 URL。
- 所有界面文本继续通过转义函数输出，防止项目字段注入 HTML。
- 本地导入项目只保存在当前浏览器；清理浏览器数据会删除导入项目，需要提前导出。
- localStorage 写入失败时提示用户导出项目，不假装已保存。
- 项目切换必须先完成数据验证；失败时保留原项目和原视图。
- 外部模型请求失败不得回退到演示结果冒充在线结果。

## 11. 架构检查清单

本轮架构评审必须逐项回答：

- 数据是否能在不改代码的情况下更换？
- 原始证据、业务对象和正式输出是否仍然分离？
- AI、规则、人和示例数据是否可区分？
- 项目切换、存档和导出是否按项目隔离？
- 无效导入是否可能污染当前状态？
- 静态正式图是否被误称为当前数据实时生成？
- 模型服务不可用时是否仍有可用的确定性路径？
- 密钥或敏感数据是否可能进入浏览器或导出包？
- 每个任务能否独立验收与回退？
- 两个数据集是否真正不同，而不是只改项目名称？

## 12. 后续决策点

本轮数据基础完成并通过测试后，需要 JIAJIA 决定：

1. 先做 D6 编排器分层，还是先做 D7 数据驱动正式 DXF。
2. 东呈是否补充真实实测数据并作为第二个正式回归样本。
3. V1A 代理交付由谁承担项目负责人和委托方代理角色。
4. 何时进入 V1B 真实项目，以及真实项目的数据使用和模型传输边界。

未经上述确认，不把原型扩展为多图种平台或长期档案系统。
