/* 配置：同源或受控跨域代理端点、受控词表与提示词。模型由服务端按任务选择。 */
const apiBase = (document.querySelector('meta[name="gujian-api-base"]')?.content || "")
  .trim()
  .replace(/\/+$/, "");

window.CFG = {
  API_PROXY: `${apiBase}/api/ai`,
  API_STATUS: `${apiBase}/api/status`,

  // 受控构件词表，用于约束识别输出，避免出现"门廊""柱子"这类通用词
  VOCAB: ["台基", "月台", "踏步", "柱础", "檐柱", "金柱", "额枋", "平板枋", "雀替",
          "斗栱", "柱头科", "平身科", "椽", "飞椽", "屋面", "正脊", "垂脊", "脊刹",
          "吻兽", "瓦作", "板门", "直棂窗", "槛墙", "隔扇", "墙体", "彩画", "匾额",
          "碑刻", "石供案", "灯笼"],
  CATS: ["台基", "柱", "枋", "铺作", "屋面木作", "屋面", "屋面瓦饰", "门窗", "木装修", "墙体", "彩画", "环境陈设"],

  // 助手的角色设定
  SYSTEM: `你是古建测绘成果工作台里的 AI 助手，与测绘院的内业人员和项目负责人一起工作。

工作分工要严格遵守：
1. 你负责看和说：读资料、认构件、解释问题、起草说明。
2. 确定性计算交给程序：数量统计、尺寸换算、规范校验、出图、组包。你不要自己算这些，也不要假装算过。
3. 下列事项只能由人决定，你必须停下来发问，不能替人决定：现场实测、存疑构件的专业判断、适用规范的选择、正式签发。

回答规则：
- 用简洁的中文，一句话说一件事，不用比喻和形容词。
- 没有实测基准时，必须说明尺寸只能是照片估算，不能作为正式成果。
- 不确定就说不确定，并说明为什么不确定，不要猜一个填上。
- 每次回答控制在 120 字以内，除非用户要求展开。`,

  /* 立项理解。用户第一句话必须真的送进模型，由它提取任务要素。
     此前这里是写死的剧本，用户说什么都回答高都玉皇庙，那是假的。 */
  intakePrompt(userText, 可用项目) {
    return `用户说：${userText}

你要从这句话里提取测绘任务的要素。只输出 JSON，不要解释：
{"对象":"","地点":"","成果类型":"","比例":"","工期":"","资料位置":"","其他要求":"",
 "缺什么":["用户没说但出正式成果必须知道的信息"],
 "匹配到的已有项目":"从下面列表里选一个 id，都对不上就填 null",
 "对用户说的话":"两三句话，说明你理解到了什么、还缺什么"}

本机现有的项目数据：
${可用项目}

规则：
1. 用户没说的字段留空字符串，不要编。比如他没说比例就别写 1:50。
2. 匹配项目要看对象名称是否真的对得上，地名相近但建筑不同不算匹配。
3. 如果匹配不上任何已有项目，在"对用户说的话"里明确说明本机没有这个项目的资料。
4. 不要假装读过任何文件。你只看到了用户这一句话。`;
  },

  /* 判断用户拖进来的这张图是什么，再决定怎么处理。
     用户不该被要求先说明"这是草图还是立面照"。 */
  classifyPrompt() {
    return `判断这张图属于下面哪一类，只输出 JSON，不要解释：
{"类别":"立面照片|细部照片|手写尺寸草图|任务书或文档|已有图纸|其他",
 "理由":"看到什么才这样判断",
 "可用于":"这张图在古建测绘里能起什么作用",
 "质量问题":["影响使用的问题，例如模糊、倾斜、遮挡、反光。没有就空数组"]}

判断要点：
- 手写尺寸草图：纸面上有手写数字和部位名称，可能有简单线条示意
- 任务书或文档：整页文字，有条款编号
- 立面照片：拍的是建筑正面全景
- 细部照片：拍的是斗栱、柱头等局部`;
  },

  /* 从手写草图照片直接读数值。此前读的是预先转写好的文字。 */
  sketchImagePrompt() {
    return `这是古建测绘现场的手写尺寸记录照片。读出上面的数值和对应部位。

只输出 JSON：
{"已识别":[{"部位":"","数值":0,"单位":"mm","依据":"纸上怎么写的"}],
 "无法确定":[{"数值":0,"可能是":[],"为什么不确定":""}],
 "读不清":["看不清的内容"],
 "说明":"一句话"}

规则：
1. 只读纸上真的写了的数字，不要按经验补。
2. 数字旁边没有写部位名称的，一律放进"无法确定"，列出可能的部位但不替人认定。这是正式成果的尺寸基准，猜错会直接进图纸。
3. 字迹潦草看不清的写进"读不清"，不要猜。
4. 单位没写的按 mm 处理，并在依据里说明这是假设。
5. 部位名称用标准名：通面阔、明间面阔、次间面阔、台基高、檐柱高。纸上写简称也输出标准名，例如"明间"输出"明间面阔"。`;
  },

  /* 从任务书照片或截图提取要求。 */
  briefImagePrompt() {
    return `这是一份测绘委托任务书的照片或截图。提取其中的要求。

只输出 JSON：
{"成果类型":{"值":"","出处":"第几条"},"比例":{"值":"","出处":""},"适用规范":{"值":"","出处":""},
 "交付格式":{"值":"","出处":""},"附带材料":{"值":"","出处":""},"精度要求":{"值":"","出处":""},
 "对象":{"值":"","出处":""},"工期":{"值":"","出处":""},
 "读不清":[],"遗漏项":[]}

只提取图上真的写了的内容。没写的字段值填空字符串，出处填"任务书未写"。看不清的写进"读不清"。`;
  },

  /* 从任务书正文提取要求。此前任务卡是写死的。 */
  briefPrompt(text) {
    return `下面是一份测绘委托任务书。提取其中的要求，只输出 JSON，不要解释：
{"成果类型":{"值":"","出处":"第几条"},"比例":{"值":"","出处":""},"适用规范":{"值":"","出处":""},
 "交付格式":{"值":"","出处":""},"附带材料":{"值":"","出处":""},"精度要求":{"值":"","出处":""},
 "遗漏项":[]}

任务书正文：
${text}

规则：
1. 只提取任务书里真的写了的内容。没写的字段值填空字符串，出处填"任务书未写"。
2. "遗漏项"里写具体缺什么，例如"未说明图层标准的具体版本"。没有遗漏就填空数组 []。
3. 不要把本提示里的字段说明原样抄回去。`;
  },

  /* 资料清单判断。此前缺口说明是写死的。 */
  materialsPrompt(list, target) {
    return `下面是一个古建测绘项目已有的资料清单。判断这些资料是否足以支持目标成果：${target || "尚未确认的古建测绘成果"}。

${list}

只输出 JSON：
{"够用":true或false,
 "主图建议":"用哪一份作主图，为什么",
 "缺口":[{"缺什么":"","为什么需要":"","不补的后果":""}],
 "说明":"两三句话给用户看"}

判断依据：正式测绘成果需要能建立比例基准的实测尺寸、覆盖完整的正立面照片、拍摄来源记录。
不要编清单里没有的资料。`;
  },

  /* 手写草图数值解析。此前三个待确认数值是写死的。 */
  sketchPrompt(text) {
    return `下面是现场手写尺寸草图的转写内容，来自测绘人员的纸质记录。

${text}

判断每个数值分别是量的哪个部位。只输出 JSON：
{"已识别":[{"部位":"","数值":0,"依据":"草图上怎么写的"}],
 "无法确定":[{"数值":0,"可能是":["按经验列两三个可能部位"],"为什么不确定":""}],
 "说明":"一句话"}

没有标注部位的数值一律放进"无法确定"，不要凭经验替人认定。这是正式成果的尺寸基准，猜错会直接进图纸。
部位名称用标准名：通面阔、明间面阔、次间面阔、台基高、檐柱高。记录里写简称也输出标准名，例如"明间"输出"明间面阔"。`;
  },

  /* 交付说明起草。此前限制条件是写死的。 */
  deliveryPrompt(ctx) {
    return `根据下面的工作区数据，起草这套测绘成果的交付说明中的"限制条件"部分。

${ctx}

只输出 JSON：{"限制条件":["每条一句话，写明限制是什么、影响哪些内容"],"说明":"一句话"}

要求：
1. 只写数据里真实存在的限制，比如哪些尺寸没有实测依据、哪些部位被遮挡未确认、材料是否有检测依据。
2. 每条要具体到部位或构件编号，不要写"部分构件"这种含糊说法。
3. 最多 5 条。`;
  },

  /* 框选说明解析。本地规则没听懂用户对框选位置的说明时，交给模型判断意图。 */
  refEditPrompt(text, 命中) {
    return `用户在古建立面照片上框选了一个位置${命中 ? "（框住了已有构件 " + 命中 + "）" : "（空白区域）"}，然后说：${text}

判断用户想做什么，只输出 JSON，不要解释：
{"动作":"新增|改名|删除|遮挡|其他","名称":"构件名称，只能从词表里选，没有就空字符串","说明":"一句话"}

词表：${window.CFG.VOCAB.join("、")}

判断标准：
- 新增：这个位置漏了某个构件
- 改名：框住的构件类别认错了，应该是别的
- 删除：这个位置其实没有构件
- 遮挡：这块被挡住或看不清
判断不了就填"其他"，不要猜。`;
  },

  /* 检查解释。检查由规则程序执行，模型只负责把结果翻译成处理说明并排优先级，
     不产生新的检查结论。 */
  checkExplainPrompt(list) {
    return `下面是规则程序对一张古建正立面测绘图的检查结果，程序算出，结果可复算。
把它们翻译成给测绘院内业人员看的处理说明，只输出 JSON，不要解释：
{"总结":"两三句话，说明整体情况和影响",
 "先处理":"某一条的编号加一句为什么先看它",
 "逐条":[{"编号":"rc3","一句话":"这条意味着什么、建议怎么处理"}]}

检查结果：
${list}

规则：
1. 只解释列表里已有的问题，不要新增检查结论，也不要替用户做决定。
2. 每条"一句话"要指出位置和后果，不要复述原文。
3. 影响正式成果有效性的问题排最前。`;
  },

  /* 修改建议提示词。要求模型输出结构化指令，前端渲染成可一键应用的卡片。
     这是"AI 提议、人确认"的实现方式：模型不直接改数据，只提出方案。 */
  editPrompt(instruction) {
    return `用户的要求是：${instruction}

请检查工作区数据，找出可以修改的项，只输出 JSON，不要任何解释文字：
{"说明":"一句话说明你发现了什么","修改":[{"对象":"构件","编号":"P03","字段":"尺寸","原值":"","新值":"","理由":"依据是哪一项数据","把握":"高"}]}

对象和字段的对应关系，只能用这几种组合：
- 对象 "构件"：编号用 P01 这种，字段只能是 名称、类别、尺寸、依据
- 对象 "实测"：编号用 d1 这种，字段只能是 部位、数值
- 对象 "现状"：编号用它的部位名，字段只能是 内容、依据

规则：
1. 只改你有依据的项。依据必须来自实测记录、任务书要求或数据本身的矛盾。
2. 没有依据就不要改，宁可少提也不要猜。把握低的也可以提，但要如实标 "低"。
3. 需要现场复核才能确定的，不要写进修改列表，在说明里单独指出来。
4. 每条理由要写清楚依据是哪一项数据，不要写"根据经验"这种。
5. 字段名只写一个词，不要写成 "尺寸|数据来源" 这种。
6. 最多提 8 条。`;
  },

  // 识别提示词
  visionPrompt(ctx) {
    return `你是中国古建筑测绘工程师，正在做正立面现状测绘。识别照片中这座建筑正立面的可见构件。
${ctx ? "已知背景：" + ctx : ""}

必须使用中国古建筑专业术语，名称只能从下列词表选择：
${window.CFG.VOCAB.join("、")}
类别只能从：${window.CFG.CATS.join("、")}

只输出 JSON，不要任何解释文字：
{"构件":[{"编号":"P01","名称":"","类别":"","位置框":[x1,y1,x2,y2],"置信度":"高|中|低","依据":"照片中看到什么才这样判断"}],
"材料":[{"名称":"","分布":""}],
"存疑项":[{"编号":"","问题":"为什么无法确定"}]}

位置框用 0 到 1 归一化坐标 [左,上,右,下]，以图片左上角为原点。
同类构件重复出现分别编号，例如四根檐柱编 P03 至 P06。
只写照片里真实看得见的东西。看不到门窗就不要写门窗，前廊敞开就如实说敞开。
被遮挡、分辨率不足或形制无法确定的，置信度标低并写入存疑项。
输出 12 到 20 个构件。`;
  }
};
