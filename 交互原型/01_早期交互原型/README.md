# 古建测绘成果工作台

可直接打开 `index.html` 浏览。若需要安装为桌面网页应用，请在本目录启动本地服务器：

```powershell
python -m http.server 4173
```

然后在 Chrome 打开 `http://localhost:4173`。

## 操作

- 使用页面底部按钮走完整流程。
- 左侧步骤可跳转到相应模块。
- 表单、复选项和上传数量保存在当前浏览器。
- 键盘左右方向键可切换页面。
- 第 8、14、18—20、23 页包含补采、返修、审核和交付分支。
