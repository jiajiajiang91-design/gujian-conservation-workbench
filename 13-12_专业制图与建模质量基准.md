# 13-12 · 古建专业制图与建模质量基准 v0.2

## 1. 结论

本项目的 CAD 图纸和三维模型不得停留在体块、轮廓或流程示意层级。

最低成品应达到 JIAJIA 于 2026-08-11 提供的六张参考图所体现的表达深度：建筑形制可辨，构件关系完整，平面、立面、剖面和三维相互一致，尺寸、标高、轴线、材料、病害与保护说明可以用于专业核对。

现有 T0-A 简化五开间样例只证明文件链路与软件兼容性。现有 T0-B 三开间样例的二维与三维分别构造，构件深度和图面完整性不足，只能记为 L0+ 技术样例。两者都不属于专业成品，不得用于宣传或交付。

当前 T0-A 通过，T0-B 未通过，T0 总门槛未通过。此前“T0 通过、T0-B 达到 L1”的结论自 v0.2 起撤销。

## 2. 依据与边界

### 2.1 依据

- JIAJIA 提供的六张古建图纸与模型参考图，定义最低视觉和专业表达深度。
- [CH/T 6005-2018《古建筑测绘规范》](https://std.samr.gov.cn/hb/search/stdHBDetailed?id=8B1827F2417CBB19E05397BE0A0AB44A)为现行行业标准，覆盖技术准备、总图、单体测绘、质量检查、验收和成果上交。
- [GB/T 50001-2017《房屋建筑制图统一标准》](https://www.cctc.cn/kjcx/kjcg/bzgf/gjbz/index.shtml)约束图幅、图线、字体、比例、符号、轴线、材料图例、尺寸、CAD 文件和图层。
- 新的国家标准《古建筑测绘规范》仍处于制定阶段。本轮不把草案当作现行依据。

参考图只用于确定验收特征，不复制其具体建筑、尺寸、纹样或图纸内容。项目成果必须来自授权资料、项目事实或明确标为 `demo` 的团队参数化数据。

### 2.2 成果等级

| 等级 | 用途 | 可以包含的内容 | 不得宣称 |
|------|------|----------------|----------|
| L0 技术链路样例 | 验证 IFC、GLB、DXF、SVG、PDF 和外部软件 | 简单几何、少量标注、技术日志 | 专业模型、专业图纸、交付成果 |
| L0+ 古建语义技术样例 | 验证古建对象分类、曲线或阵列、图层和多格式生成 | 简化构件、示意视图、兼容性记录 | L1 局部专业样板、同源剖切、专业图面 |
| L1 局部专业样板 | 验证一组古建构件、屋面和详图能力 | 完整局部构造、来源和未知项 | 完整单体成果 |
| L2 单体专业成果 | 完成一座资料充分建筑的全套单体成果 | 三维、总平或定位、平立剖、详图、表格和报告 | 正式签发，除非责任条件满足 |
| L3 正式成果 | 责任人员审核、签名、验签并发布 | L2 全部内容和正式责任记录 | 超出资料、规范和签发范围的结论 |

T0-A 只验收 L0。L0+ 不构成任何任务门槛通过。T0-B 必须完成 L1，并证明技术路线可以达到 L2。T10 和 T14 必须在资料充分的完整样本上达到 L2。

## 3. 三维模型最低要求

### 3.1 形制与构件

模型必须按项目实际形制表达，不得用统一的矩形体块和双坡平板屋面代替古建构造。

资料中存在的下列对象应建立独立、可选择、可追踪的构件：

- 台基、阶梯、地面、散水、栏杆和附属墙体。
- 柱、梁、枋、檩、椽、望板及其上下、内外关系。
- 斗栱、撑拱、雀替或其他承托构件；建筑本身没有时标记为不适用。
- 屋面曲线、举折或举架控制、檐口、戗脊、正脊、垂脊、瓦面和饰件。
- 墙体、门、窗、槛墙、隔扇、格心或花格。
- 楼层、回廊、附属屋面和相接建筑。

每个构件保存稳定 `entityId`、类型、位置、几何、材料、来源和状态。重复构件使用可复用族或实例，不复制成失去语义的散乱网格。

### 3.2 几何与来源

- 屋面曲线使用可追溯的控制点、曲线或曲面，不用视觉近似替代可用测量。
- 三维模型与二维视图使用同一 `GeometryRevision`。
- 剖面必须由三维构件真实剖切，不能另画一套不相干的二维图。
- 二维生成器不得根据跨度、屋面公式或尺寸参数重新画建筑几何。平面、立面、剖面和详图中的结构性图元必须来自同一几何版本的投影、剖切或受控展开。
- `measured`、`model`、`rule`、`human` 和 `demo` 内容在数据中可区分。
- 资料不支持的暗构造保持未知、局部缺口或推断状态，不补默认斗栱、椽数、瓦数或基础做法。
- 模型节点可从三维视图追溯到事实、证据和人工决定。

### 3.3 三维验收

- IFC 保留对象分类、稳定 ID、属性和来源引用。
- GLB 保留与 IFC 对应的稳定节点 ID，可在网页中选择构件。
- 模型在独立查看器中无破面、反面、无限值、严重穿插和非预期悬空。
- 构件数量、总体尺寸、关键标高和轴网与项目事实一致。
- 预览至少提供轴测图、正交视图、剖切和按来源状态着色。
- 每个构件记录 `geometryStatus`：`resolved`、`simplified`、`placeholder` 或 `unknown`。L1 必需构件必须为 `resolved`；简化体块和占位构件不能计入 L1。
- 屋面、柱梁枋、檩椽、承托、柱础、台基和基础形成可查询的连接关系。非预期悬空和穿插必须为零。

## 4. 图纸最低要求

### 4.1 图种

资料充分的单体至少包括：

- 总平面或可追溯的建筑定位图。
- 各层平面、屋顶平面和必要的仰视平面。
- 任务要求的各向立面。
- 穿过主要结构、屋面和高差关系的横剖面与纵剖面。
- 屋脊、檐口、斗栱、柱础、门窗、栏杆或其他关键构件详图。
- 门窗表、材料表、病害表和图纸目录。
- 现场照片或证据索引与图纸对象的对应关系。

某一图种不适用时记录 `notRequired`。资料不足时记录明确阻断码，不得输出空图或用通用构造补齐。

### 4.2 图面内容

每张图按用途至少检查：

- 图名、图号、比例、图幅、图签、修订和打印设置。
- 定位轴线、轴号、剖切符号、索引符号、详图符号和北向。
- 总尺寸、轴线尺寸、开间或进深尺寸、构件定位尺寸和必要净尺寸。
- 室内外标高、檐口、屋脊、楼层、台基和基础标高。
- 粗细线层级、可见线、剖切线、投影线、隐藏线、中心线和辅助线。
- 材料边界、剖面填充、地面线、墙体、木构、瓦面和基础做法。
- 构件名称、规格、做法、病害、修缮或保护说明。
- 现场事实、推断、拟修内容和新作内容的图层或符号区分。
- 尺寸和说明不得遮挡主体，密集区域使用索引详图或分图表达。

### 4.3 参考图对应关系

| 参考特征 | 本项目最低验收 |
|----------|----------------|
| 照片与测绘图并置 | 证据查看器能从照片定位到图纸对象；交付包含照片索引 |
| 古建屋面和装饰完整 | 屋面曲线、瓦垄、屋脊、檐口和实际存在的饰件可辨 |
| 木构与门窗细节完整 | 柱梁枋、斗栱或承托、门窗分格和格心不是空白矩形 |
| 剖面展示真实构造 | 屋面、承重木构、楼地面、台基和基础关系来自同源剖切 |
| 多级尺寸和标高 | 轴网、开间、总尺寸、关键构件和主要标高可以复核 |
| 修缮说明和引线 | 说明关联对象、病害或构造，不是悬空文字 |
| 三维材质和构件层次 | 颜色只辅助识别；对象语义和构件关系不能依赖贴图 |

## 5. CAD 文件最低要求

DXF 必须是可继续编辑的工程文件，不是图像、单一块或大量无语义碎线。

- 模型空间按 1:1 和统一单位建立。
- 图纸空间包含正确图幅、图签、比例视口和打印样式。
- 重复构件使用块；门窗、斗栱或栏杆实例保留对象引用。
- 尺寸使用 `DIMENSION`，文字使用 `TEXT` 或 `MTEXT`，材料使用 `HATCH`，重复图元优先使用 `INSERT`。
- 图层至少区分轴线、剖切、轮廓、投影、隐藏、尺寸、文字、填充、现状、病害、拟修和图框。
- 中文字体不得替换成缺字字体；PDF 打印不得乱码、裁切或丢线。
- AutoCAD 与另一种独立 CAD 工具均能打开、审计、选择、编辑、保存和打印。
- 反向解析后能恢复图层、图块、标注、布局、视口和对象来源关系。

对象数或线条数不是质量指标。通过标准是语义完整、关系正确、图面清楚和可继续编辑。

## 6. 自动检查与人工审核

### 6.1 自动检查

自动检查至少覆盖：

- 项目事实、三维、平面、立面和剖面之间的尺寸与对象一致性。
- 视图是否为空、是否越界、比例是否可读、文字和尺寸是否重叠。
- 图层、线型、线宽、字体、块、填充、标注、视口和打印设置。
- IFC、GLB、DXF、SVG 和 PDF 的解析、哈希、对象映射和来源引用。
- 必要构件和图种是否生成，或是否有可解释阻断。
- 未知、推断、演示和不具备正式资格的内容是否被清楚标记。
- IFC、GLB、DXF、SVG 和 PDF 引用同一 `geometryRevisionId`。
- 除图框和注释外，结构性图元的 `sourceEntityId` 覆盖率为 100%。
- 用独立几何程序重算剖切，核对构件集合、轮廓、轴网、开口、标高和关键尺寸。
- 每张图按 `DrawingRequirement` 检查图种、尺寸类别、标高类别、构件、说明、来源和阻断，不按图元总数判断质量。

对象数、图元数、文件大小和软件能否打开只作为诊断或兼容性证据，不得作为 L1、L2 或 L3 的通过条件。

### 6.2 人工审核

人工只处理两类工作：

1. 对资料无法唯一决定的构造、保护判断和高风险问题作专业决定。
2. 对成组三维与图纸预览进行最终专业复核。

审核人员不重复录入系统已经掌握的尺寸、身份和来源。修改意见应定位到对象、视图或说明，并只使受影响成果失效。

## 7. 验收门槛

### 7.1 T0-A 技术链路

- 一份简单参数化样例完成 IFC、GLB、DXF、SVG 和 PDF 往返。
- 稳定 ID、取消、失败清理、AutoCAD 审计和独立三维查看器通过。
- 报告显著标注 `L0 技术链路样例`。

T0-A 通过不代表专业图纸或模型通过。

### 7.2 T0-B 局部专业样板

使用团队自有 `demo` 数据完成一个可重复生成的古建局部样板，至少包括：

- 一段有曲率的屋面、瓦面、屋脊和檐口。
- 一榀柱梁枋、檩椽及实际采用的承托构件。
- 一组有分格和格心的门窗或隔扇。
- 台基、柱础和基础剖面。
- 同源三维、立面、横剖面、构件详图和可编辑 DXF。
- 尺寸、标高、轴线、材料、来源状态和至少一条病害或保护说明。

样板还必须满足以下门槛：

- 选定的一榀“屋面—瓦作—檩椽—梁枋—承托—柱—柱础—台基—基础”构造链完整；不适用项有明确依据。
- 矩形体、圆管、平板和合并网格只能标为 `simplified` 或 `placeholder`，不能冒充瓦件、斗栱、承托或节点详图。
- 横剖面由三维构件与剖切面真实求交生成；立面和屋顶平面由同一几何版本投影生成。
- 必需视图的结构性图元全部引用源构件，独立重算结果在声明容差内一致。
- 尺寸按总尺寸、轴线、开间或进深、构件定位和关键净尺寸分类检查；标高覆盖室内外、台基、檐口、屋脊和关键构件。
- 图签包含项目、图名、图号、比例、单位、状态、修订、日期和责任边界；打印字高、线宽、比例、视口和遮挡通过模板检查。
- 轴测、正交、真实剖切和来源状态四类预览可以检查内部关系。
- 独立验证器根据冻结样本和成组成果计算资格。生成器不得自行写入 `L1` 并以该字段作为通过证据。
- 独立专业复核保存审查人、版本、缺陷位置和 P0、P1、P2；存在任一 P0 时，`localProfessionalSampleEligible` 必须为 `false`。

自动检查、AutoCAD、第二 CAD 工具、独立三维查看器和人工成组预览全部通过后，T0 才整体通过。软件能打开、审计和打印只证明兼容性，不能替代专业复核。

### 7.3 T10 与 T14

- 资料充分的团队参数化单体达到 L2。
- 第二套完整工程样本按相同代码路径达到 L2，或对真实缺失项返回阻断。
- 南禅寺局部样本只生成证据支持的局部成果。
- 每套样本按“样本 × 图种 × 质量检查”形成成果矩阵。
- 任何空图、默认古建构件、硬编码项目分支或单独补画的剖面都判定失败。

## 8. 当前决定

1. 保留现有 T0-A / L0 与 T0-B / L0+ 文件作为历史技术证据，不删除、不覆盖。
2. 撤销现有 T0-B 的 L1 资格和 T0 总通过结论。依赖 T0 的任务不得标记验收完成。
3. 重做 T0-B。它不要求完成整栋 L2，但必须把选定构造链、同源剖切和专业图面做完整。
4. T10 负责完整图种生成，T14 负责两套完整项目和一套局部项目的专业验收。
5. JIAJIA 提供的六张参考图与 2026-08-11 小红书浏览结果只用于视觉深度对照，不进入代码、样例项目包和交付包，也不作为尺寸、形制或版权来源。
6. 未经实测、专业确认和责任签发的结果继续标为 `demo` 或代理成果。
