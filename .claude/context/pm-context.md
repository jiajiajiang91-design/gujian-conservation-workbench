# 产品领域知识


## T0-B 主视图进度（2026-08-11）

- 平面、屋顶平面、南立面、横剖、纵剖和轴测已从几何版本 `3788f4e4-339c-568d-aa58-f74b36b23c5a` 生成 ViewGeometry，视图合同版本为 `04d00093-0bec-5f2a-bc68-def2a292c932`。
- 三张剖切视图均采用真实网格求交。剖后投影通过投影三角面覆盖区间和深度差解析计算，不再使用固定步长判断遮挡。
- 屋顶平面、南立面和轴测从同一源网格生成完整候选边与可见区间。屋面下木构、北排遮挡构件和基础等禁入内容不会透出；同一共线边只保留唯一来源。
- 独立审核复查八万多个投影采样点，隐藏线泄漏为 0；切面构件集、闭合区域类型和二维边界与冻结答案一致。
- 定向源网格包已冻结 SHA-256。单个面反序、顶点变化、缺失或重复构件、构件类型、来源或关系变化都会阻断生成。
- 独立验证器核对视图框架、剖面、结构线来源、二维与三维坐标、边界引用、材料和结果哈希。重算哈希后篡改坐标框架或材料仍会失败。
- 四张详图合同已冻结锚点、选择集、切面或投影类型、精确可见线答案、材料优先级、关系链和门窗拓扑。详图 ViewGeometry 已生成，独立验证 73 项检查与 14 项篡改负例全部通过。
- DrawingPackageContract 与中间 IR 已冻结，合同版本为 `176ee4f0-9cbd-51ba-939e-aa2cad36a307`。IR 绑定十张 ViewGeometry、两张 A1、结构线、材料区、标注语义和来源 sidecar；独立验证基线 5 项与篡改负例 28 项全部通过。
- 原生 DXF 已由同一 IR 生成：R2018、模型空间 1:1 mm、两个 A1 布局、十个锁定视口、原生 DIMENSION/HATCH/INSERT/MTEXT、结构 XDATA 与来源 sidecar。独立验证 10 项与 13 项篡改负例通过；AutoCAD 2024 Core Console `AUDIT` 为 0 个错误、0 项修复、0 个删除对象。
- 当前状态是 `passed-section-geometry-only / passed-projection-geometry-only / passed-detail-geometry-only / passed-contract-and-ir-only / passed-native-dxf-only / generated-not-qualified / L1=false`。原生 DXF 只是未资格化的单项输出，不是完整图纸包。
- AutoCAD 2024 已通过 `AUDIT`，结果为 0 个错误、0 项修复、0 个删除对象。QCAD 3.32.9 可以打开、编辑、保存和打印，但保存后会改写 33 个原生尺寸并解除 10 个用户视口的锁定。QCAD 不支持当前成果的无损往返，副本不能替代规范 DXF。
- 下一任务绑定许可字体，由同一 DrawingPackageIR 生成 SVG、两页 PDF 和双 A1 整图，再完成成组专业复核。
本文件是产品状态的唯一权威入口。产品范围、阶段和决策变化时，以本文件为准。

## 一、当前决策

- `13-8_核心PRD_古建保护成果工作台.md` v1.0 已由 JIAJIA 于 2026-08-10 确认，是技术架构的产品需求基线。
- 核心 PRD 描述终局产品。产品从项目资料生成可追溯三维模型、平面、立面、剖面、详图和专业 CAD，并支持检查、签发准备、交付和持续更新。
- AI 先处理资料整理、专业存疑、规则冲突和风险问题。满足证据、规则、可逆性、授权和模型任务质量门槛的事项自动执行；专业人员主要审核成组变更和成果预览。
- 人工保留三类责任：补充系统无法获得的现场事实、选择没有唯一答案的规范或专业方案、完成正式签发。
- AI 的系统提示只定义角色、任务目标和事实责任边界。确定性业务要求进入项目数据、工具参数和检查程序，不设置 120 字等能力限制。
- 目标模型为 Kimi K2.6，官方模型标识为 `kimi-k2.6`。所有 AI 任务不再回退到 `moonshot-v1-*`。模型密钥和受控接口地址只能保存在服务端。正式接入需要在架构确认后实施。
- `13-9_技术架构_古建保护成果工作台.md` v1.1 已由 JIAJIA 于 2026-08-11 确认。方案 B、Python 制图 worker 和本地模式正式签发锁定均已接受。现有 React 工作台只作为迁移和问题证据。
- PRD 与架构文档已经确认。JIAJIA 于 2026-08-11 否定现有 CAD 与模型质量；独立复核确认 T0-B 的二维与三维分别构造，不能达到 L1。当前 T0-A 达到 L0，T0-B 降为 L0+ 且未通过，T0 总门槛未通过。
- JIAJIA 提供的 `寺庙古建筑设计方案图.dwg` 只作为外部制图参照。文件显示平立剖、构件节点和多比例成组表达，但缺字体、单位声明、来源许可和测绘证据，不能进入项目事实或样例数据。
- JIAJIA 提供的 `一套完整的古建施工图.dwg` 只用于校准成组图种、标注密度和节点层级。该文件四个图框均位于模型空间，缺少原生尺寸、有效纸空间视口、单位声明和字体闭包，不能作为原生 CAD、三维同源或测绘金标准，也不得进入生成依赖。

## 二、产品定位

> 面向古建测绘与保护项目团队，把照片、测量记录、历史图纸和任务要求转成可追溯的建筑数据、三维模型和专业图纸，经专业复核形成可交付、可归档、可持续更新的成果。

产品名称为“古建保护成果工作台”。产品形态是正式 toB 工作台，不是单项目演示页或后台管理系统。

## 三、目标用户

| 角色 | 核心任务 | 产品责任 |
|------|----------|----------|
| 项目负责人 | 设置范围、规范、成果和责任角色 | 管理项目与组织签发 |
| 测绘与建模人员 | 导入资料、核对对象和补充现场事实 | 保证资料与模型可用 |
| 专业审核人 | 审核成组变更、三维和图纸预览 | 处理非唯一专业选择 |
| 档案接收人 | 核验清单、版本、来源和签发 | 接收或退回指定成果 |

买单方可能是乙方企业、项目总包方或文物部门。真实采用和付费仍需试点验证。

## 四、当前阶段

当前暂停 T3，先重做 T0-B 专业质量验证。后续仍不扩展旧工作台。

1. JIAJIA 已确认终局核心 PRD v1.0。
2. 目标技术架构 v1.1 已确认，独立审查结果为 P0 和 P1 均为 0。
3. T0-A 已完成 Windows 上的 IFC、GLB、DXF、SVG 和 PDF 技术链路。T0-B 已完成同源三维、六个主视图、四张详图 ViewGeometry、制图包合同、中间 IR 和原生 DXF。AutoCAD 审计通过，QCAD 无损往返不通过。许可字体、SVG/PDF、成组图纸和专业复核尚未完成，因此 T0 仍未通过。
4. T1 保留现有 13 项旧代码测试和 17 项单元测试，补充迁移特征测试，只锁定旧行为，不继续修改旧运行架构。
5. T2 已形成代码提交和测试记录，但因 T0 前置门槛未通过，不标记为实施验收完成。代码保留，不回退，不继续扩展。
6. 按 `13-12_专业制图与建模质量基准.md` v0.3 重做 T0-B。v2 同源语义三维、十张 ViewGeometry、制图包合同、中间 IR 和原生 DXF 已完成。视图合同包含十个视图框架、稳定的 `x=-1750 mm` 横剖、`±0.5 mm` 切面复算、四张详图的精确来源与材料 oracle、标注安全区和两张 A1 布局。剖切与投影均按视图限定表达范围，避免重复瓦件、椽网格、三角内部边和遮挡构件污染图面。当前几何版本为 `3788f4e4-339c-568d-aa58-f74b36b23c5a`，视图合同版本为 `04d00093-0bec-5f2a-bc68-def2a292c932`，制图包合同版本为 `176ee4f0-9cbd-51ba-939e-aa2cad36a307`。状态仍为 `generated-not-qualified`、`L1=false`。许可字体、SVG、PDF、成组图面和独立专业复核通过，并确定满足无损往返要求的第二 CAD 后，才恢复 T3。
7. 后续使用公开实测样本验证资料输入、建模、平立剖出图、检查、导出和回导；南禅寺只用于内部局部领域测试。

## 五、输入与成果

最低专业验证输入包括：

- 明确的建筑与任务要求。
- 带来源的现场照片。
- 带单位、测量人、时间、方法和原记录的关键尺寸。
- 可以说明空间、开口和构造关系的图纸或现场草图。
- 适用规范、成果目录和责任角色。

资料不足时，系统生成缺失清单和带未知状态的参考成果。无实测依据的内容不能标为现场测量事实。

终局成果包括：

- 带对象和证据关系的三维建筑模型。
- 总平面、各层平面、屋顶平面、各向立面、关键剖面和构件详图。
- 可编辑 CAD、三维交换文件、PDF 或 SVG 预览、结构化项目数据、检查报告和交付清单。
- 签发、接收、退回、版本和审计记录。

## 六、AI、程序与人工

| 环节 | AI | 确定性程序 | 人工 |
|------|----|------------|------|
| 资料 | 理解、分类、转写和关联 | 校验文件、哈希和单位 | 提供现场事实 |
| 建模 | 识别对象、关系和候选 | 求解三维几何和视图 | 审核关键预览 |
| 问题 | 分析存疑、冲突与风险，形成方案 | 验证规则和影响，执行可逆操作 | 审核非唯一选择 |
| 制图 | 组织任务、说明和工具调用 | 生成专业图纸和文件 | 审核并签发 |

模型结果、规则结果、人工决定和演示数据必须分别记录。系统事件只进入审计，不作为业务事实来源。

## 七、验证样本

第一套工程流程样本采用 Locke 镇 HABS 调查中的 Dai Loy Gambling Museum 子集。资料包含照片、文字、尺寸、平面、立面和剖面，体量适合第一次专业制图测试。详见 `13-11_正式制图能力与验证样本预研.md`。

6 个公开样本文件已保存到 `验证材料/05_公开实测样本/Dai_Loy_HABS/`，合计 1,354,283 字节，并完成来源、哈希、PDF 页数和视觉检查。样本尚未接入产品项目包，也未生成新 CAD。

该样本是美国华人木构建筑，只验证资料到 CAD 的工程流程。高都和东呈的演示数据不能代替实测样本。

第二套内部领域样本采用南禅寺大殿。公开论文提供柱网、柱高、柱截面、部分屋架、铺作和材分数据，仓库已有三张 CC BY-SA 4.0 照片。结构化转录保存于 `验证材料/05_公开实测样本/南禅寺_公开研究数据/`。

南禅寺论文 PDF 和图版受版权保护，不进入仓库。数据只用于内部研究，外部发布前需要完成权利核对。样本缺少原始点云、现场记录、完整构件拓扑、屋面和节点资料，只能验证来源、数值约束和局部多视图，不能验证完整专业交付或取得正式成果资格。

敬罗家塾不作为当前可运行样本。香港记忆只公开两张人工绘图过程图片，记录标记为不可下载，版权仅允许站内使用。完整测绘图需要另行申请。

Ortho2CAD 和 Drawing2CAD 可作为图纸转参数模型的研究参考，但两者面向机械零件，尚未处理古建语义、来源关系或建筑专业出图，不能直接作为产品建模内核。

## 八、现有成果的状态

- Git 基线提交为 `ea8f8f4`，标签为 `baseline/2026-08-09-pre-rebuild`，当前分支为 `rebuild/product-workbench`。
- 高都与东呈数据均属于演示或代理数据，没有足够原始实测证据，不得作为正式成果。
- 现有 React 工作台包含结构化数据与代理文件实验，但资料输入、泛化、来源关系、成果失效和模型接入未达到当前 PRD。
- 旧 `js/config.js` 的长提示和 120 字限制不再代表产品方向。
- 现有助手显示本地固定建议，没有调用真实模型。

## 九、当前待验证问题

1. 专业人员是否接受 AI 自动处理可逆事项，并以成组预览代替逐项确认。
2. 照片、测量和历史图纸能否稳定建立同一建筑对象与三维几何。
3. 开放几何与 CAD 工具能否达到事务所可继续编辑和打印的质量。
4. 中国古代木构的构件、屋面和空间关系需要哪些专用数据与规则。
5. 单项成果的模型费用、人工修改量和总耗时是否低于人工基线。
6. 项目资料的权属、保密和模型传输是否允许真实使用。
7. 真实项目是否产生继续使用、询价、采购测试或付费行为。

## 十、正式文档优先关系

1. 当前产品范围、AI 自动处理原则、人工责任和验收以 `13-8_核心PRD_古建保护成果工作台.md` v1.0 为准。
2. 专业制图与建模最低成品标准以 `13-12_专业制图与建模质量基准.md` 为准。
3. 模型接入、开放制图技术和验证样本依据以 `13-11_正式制图能力与验证样本预研.md` 为准。
4. `13-9_技术架构_古建保护成果工作台.md` v1.1 是当前实施依据。
5. `13-10_V1A数据基础开发验收记录.md` 与现有代码只作为迁移和问题证据。
6. `12_调研结论.md` 和 `13-1` 至 `13-7` 保留历史分析；与本文件冲突时，以本文件和最新 PRD 为准。
