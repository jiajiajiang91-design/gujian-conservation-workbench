# 03 · 开源 agent 技术借鉴（deepseek-harness 对照研究）

> 本文为 2026-08-14 的研究记录。五条设计原则与借鉴条目 H1 至 H7 仍有效；文中描述的实现状态为研究时点状态，当前状态见 [技术架构](../02_技术/01_技术架构.md) 第 2 章。


> 研究对象：deepseek-harness（DeepSeek 官方开源 agent 框架，本机克隆于 `D:\Claude_jiajia\10_工具环境\deepseek-harness`，GitHub 约 94.8k star，developer preview 阶段。来源：github.com/deepseek-ai/deepseek-harness，2026-08-14 访问）。
> 研究依据：该仓库 `docs/subsystems/` 下的官方中文子系统文档（tools、approval、plan、permission-presets、skills、compaction、session），文档中的类型块由仓库脚本与源码自动比对，可信度较高。本文未逐行核对全部源码，属于文档级研究，需要落码时按文中给出的源码路径复核。
> 对照基准（研究时点 2026-08-14）：界面与交互形态第八节表 10（当时的第 1 代动作清单）、辅导 11 要点笔记、Codex 接手复查清单、`apps/server/src/kimi-gateway.ts`（当时实现）。
> 编号对照：本文正文沿用研究时的旧编号，13-5 即现 [06 用户旅程](06_用户旅程.md)，13-6 即现 [07 界面与交互形态](07_界面与交互形态.md)，13-2 即现 [01 可迁移 AI 能力与流程影响](01_可迁移AI能力与流程影响.md)。现行动作清单以 07 第八节为准。

## 0. 总结论

harness 的价值不在架构而在五条可移植的设计原则：模型可见字段与程序执行字段严格分离；确认结果是封闭集合且异常一律按拒绝处理；软性引导与硬性强制分层且互不读写；全部行为落在只追加的事件日志上，任何状态都是日志的重放结果；参考指令按目录加载，正文按需读取。这五条与辅导 11 的方向（Agent 定义做减法、确定性要求进程序、场景文件按需读取）互相印证。当前工作台的 KimiGateway 是纯文本调用，表 10 的动作分发尚未实现，因此本文的产出是动作层的先行设计修订，不是对已有代码的返工要求。

## 1. 对照一：动作怎么定义

结论：表 10 的五个字段（动作、触发方式、前置条件、确认级别、留痕）覆盖了业务侧，缺的是接口侧的三样东西：参数定义、结果结构、失败行为。harness 的工具定义把这三样作为必填项。

### 1.1 harness 的做法（来源：docs/subsystems/tools.zh.md，源码 packages/core/tools/src/index.ts）

1. 一个工具由两部分组成。模型可见部分只有三个字段：名称、描述、参数 schema，经白名单投影后进入模型请求。执行部分（执行函数、输出契约、超时、并发标记、界面展示回调）永不发给模型。
2. 参数有类型化 schema，框架代为校验。参数不合规不执行动作，返回结构化错误 INVALID_ARGS 给模型，模型据此自行重试。没有关键词退路这一层。
3. 输出也有契约。每个工具必须声明输出 schema，执行结果不合规同样按错误处理（INVALID_TOOL_OUTPUT）。结果里模型可见内容与程序使用的规范值分开。
4. 参数一旦进入流水线不可改写，理由写得很明确：历史记录、审计、界面展示和实际执行必须看到同一份参数。
5. 未知动作返回结构化错误 UNKNOWN_TOOL，调用失败但对话不中断。

### 1.2 与表 10 逐字段对照

| 表 10 现有字段 | harness 对应物 | 判断 |
|---|---|---|
| 动作、触发方式 | 名称 + 描述（模型靠描述路由） | 已覆盖。触发方式本质是描述的一部分 |
| 前置条件 | guard（程序化校验，只能拒绝不能放行，见对照二） | 表述在，但需改写为可程序校验的条件 |
| 确认级别 | 审批 seam（见对照二） | 已覆盖，需补异常默认行为 |
| 留痕 | 事件日志（见对照三） | 已覆盖，需补模型原文留存 |
| （缺）参数定义 | 类型化参数 schema + 校验 | 表 10 需新增。例如"生成修改建议"的参数是哪几个字段、哪些必填 |
| （缺）结果结构 | 输出契约 + 模型可见内容与程序值分离 | 表 10 需新增。每个动作执行完返回什么结构，供界面渲染和留痕 |
| （缺）失败行为 | 结构化错误回给模型重试 | 表 10 需新增。参数错、前置不满足、执行失败各自返回什么 |

### 1.3 对关键词退路的重新定位

13-6 现文写"模型将用户表达映射到清单动作，失败时退回关键词匹配"。harness 的做法提示了一个更细的分层：模型输出格式错误或参数不合规时，第一选择是把结构化错误回给模型重试一次，重试仍失败才落关键词匹配；模型服务整体不可用时才直接走关键词。理由是模型自我纠错的成功率远高于关键词对自然语言的覆盖率。这是设计判断，标注为假设，验证方式是动作分发实现后用同一批真实用户表达对比两种退路的成功率。

## 2. 对照二：执行前确认与留痕

结论：表 10 的确认级别缺两条规则，一是异常情况的默认行为，二是授权的作用范围。harness 的审批设计恰好补上这两条，并且直接印证了"浏览不设硬锁、停靠点只管写入和推进"的分层。

### 2.1 harness 的做法（来源：docs/subsystems/approval.zh.md、plan.zh.md、permission-presets.zh.md）

1. 确认结果是封闭集合：允许一次、拒绝、撤回、通道不可用，共四种。除"允许一次"外全部按拒绝执行。确认弹窗没弹出来、用户关掉页面、程序异常，都不会放行动作。文档称之为 fail closed。
2. 一次授权只放行被询问的那一个动作。不存在"这次同意了就默认后面都同意"的隐式升级；要放宽只能显式切换会话级策略，且切换本身记入日志。
3. 每次确认在日志里留两条成对记录：询问一条、决定一条，用同一个请求 id 关联。程序崩溃时留下的是"有询问无决定"的可检测状态，而不是凭空出现的决定。
4. 确认请求不复制动作参数，只引用已经展示给用户的那次调用。避免弹窗里的参数副本与实际执行的参数漂移。
5. 软硬分层：计划模式（相当于流程引导）是纯软性提示，只改系统提示词段落，不增删动作目录，也不做任何强制；真正的强制由沙箱和审批两个独立开关承担，三者互不读写。进出计划模式时动作目录保持不变，避免模型因目录变化而困惑。
6. 权限预设：把多个独立开关打包成具名档位（例如"工作区可写 + 逐条确认"），用户选档位，程序拆开写入各开关，档位选择本身也记日志。

### 2.2 对古建的映射

1. 表 10 的"逐条确认"需要补一句：确认通道异常时按拒绝处理，且一次确认只对当前这一条建议生效。这两句现在不写，实现时就会被默认成宽松行为。
2. "推进流程"动作在停靠事项未清时"拒绝并说明原因"，这个语义与 harness 的 guard 一致（guard 只能拒绝、不能放行，多条 guard 里任何一条拒绝即拒绝）。可以明确为原则：前置条件全部实现为程序校验，模型无权绕过。这正是辅导 11 "确定性要求迁移到规则或脚本"的具体形态。
3. 浏览引导与写入确认分层：浏览环节的引导（该看什么、下一步去哪）做成提示层，不锁任何动作；写入、推进、签发做成确认层，走留痕。环节切换时动作清单不增删，用前置条件控制可用性。这与 13-6 现有设计方向一致，harness 提供的是"两层互不读写"这条更严格的边界。
4. （可选）toB 角色档位：测绘员、审核人、项目负责人各对应一组确认级别配置，用户按角色选档，程序拆开落到各动作。属于 PRD 阶段议题，第 1 代六动作不需要。

## 3. 对照三：上下文管理与长对话

结论：harness 把"只追加的事件日志"作为唯一真源，模型上下文和界面展示都是日志的派生视图，这个概念与档案行业对过程真实性的要求天然契合，值得作为古建留痕设计的统一原则；压缩和按需加载两项机制当前用不上，但其中两条原则应现在定下。

### 3.1 harness 的做法（来源：docs/subsystems/session.zh.md、compaction.zh.md、skills.zh.md）

1. 会话是只追加的事件日志，带连续序号。模型消息历史从日志派生，从不单独存储；每个模型请求的完整信封（配置、系统提示词、工具清单）也记入日志，因此任何一次请求都可以从日志完整重建。
2. 模型请求的动作调用以原文记录：模型输出的参数 JSON 字符串一字不改存档，解析在存档之后。解析失败也能追溯模型到底说了什么。
3. 长对话压缩：摘要替换模型可见的历史区间，但被替换的原始事件仍留在日志里（称为被遮蔽，不是被删除）。压缩操作本身用成对的开始与结束事件括住，中途崩溃留下可检测的未闭合标记，而不是伪造的完成状态。
4. 参考指令（skill）渐进加载：模型上下文里只放目录（名称加一句描述），正文在模型点名时按需读取，每次读取都取当前最新版本。

### 3.2 对古建的映射

1. 统一留痕原则：AI 对话、动作执行、确认询问与决定、流程推进，落到同一条项目内只追加的事件流上，事件带序号和时间，成果版本引用事件区间。当前工作台的修改历史、调用记账、GeometryRevision 分开存放，概念上先统一（是否物理统一是架构问题，留到 PRD 后）。产品叙事上这一条有独立价值：文物归档本身要求全程可追溯，事件溯源等于把档案学的过程真实性做进产品底座。
2. 调用记账应保存模型原始输出原文，解析后的结构另存。现在就能定的原则，成本低。
3. 两条现在就该定的压缩原则（实现可以推迟）：原始记录只从模型上下文移除、永不从项目留痕删除；任何压缩或整理操作自身留痕。
4. 场景文件按需加载：古建 agent 的测绘规范、构件词表、成果模板等参考材料拆成场景文件，主提示词只放一行目录，按环节读取。这是辅导 11 "按工作场景拆分参考文件"的现成参照实现，也是 Agent 定义做减法的具体做法。

## 4. 明确不建议照搬的部分

结论：harness 是面向开发者的通用 agent 平台，以下机制服务于它的通用性，古建 agent 是单一垂直场景，照搬会违背 0 到 1 最小实现原则。

1. 插件架构（Cordis、能力 seam、声明合并）：解决"任何能力都可替换"的平台问题，古建不需要。
2. 作用域分层（scope、子 agent 的工具继承与遮蔽）：多 agent 调度的基础设施，是否拆多 agent 本身在辅导 11 中已明确不预设。
3. 并发调度（并行工具组、独占屏障）：第 1 代六动作串行执行即可。
4. Code Mode、沙箱执行、MCP 接入：与古建动作清单无关。
5. 压缩的完整实现（token 压力测量、工具结果修剪）：古建单次工作会话的对话量远未到需要的规模，只取第 3.2 节两条原则。

## 5. 借鉴任务清单

借鉴条目分设计层与实现层两类。H1 至 H3 为文档修订，H4 与 H5 为动作分发的实现要求，带（可选）标记的条目默认不做。

### H1 修订 13-6 表 10（设计层，约半个工作段）

- 做法：为六个动作补三列。参数定义（字段、类型、必填）、结果结构（执行完返回什么，界面渲染和留痕共用）、失败行为（参数错、前置不满足、执行失败各返回什么，是否回模型重试）。前置条件列逐条改写为程序可校验的表述。
- 同节补三条原则：模型可见字段只有名称、描述、参数；参数进入执行后不可改写，留痕、展示、执行看同一份；未知动作返回结构化错误且对话不中断。
- 验收：六个动作每个有完整的八项字段；新增原则与现有"字段白名单、编号检查"段落合并，无重复表述。
- 若第 6 节动作全集获 JIAJIA 确认，21 个动作全部按八字段格式一次入表，每行标注实现批次；模型目录只投影已实现动作（定义一次到位、实现分批、暴露只含已实现，依据见 6.2）。

### H2 补确认机制设计（设计层，约四分之一工作段）

- 做法：在 13-6 第八节补一段确认规则。确认结果为封闭集合（允许一次、拒绝、用户撤回、通道不可用），除允许一次外全部按拒绝执行；一次确认只对当前一条生效；确认询问与决定成对留痕并以同一 id 关联；确认界面引用已展示的动作卡片，不另行复制参数。
- 验收：四条规则入文，与表 10 确认级别列互相引用。

### H3 写明软硬分层原则（设计层，约四分之一工作段）

- 做法：在 13-6 交互假设或第八节补一段。浏览引导为软性提示层，不锁动作、不留痕；写入、推进、签发为硬性确认层，走 H2 规则；环节切换不增删动作清单，可用性由前置条件表达。
- 验收：与"浏览不设硬锁，停靠点只管写入和推进"的既有表述合并为一段完整原则。

### H4 动作分发实现方案（实现层，时点待定，依赖 H1）

- 做法：KimiGateway 增加函数调用支持时，按 H1 修订后的表 10 生成模型侧动作清单（只投影名称、描述、参数）；参数校验失败将结构化错误回给模型重试一次，二次失败落关键词匹配，模型服务不可用直接走关键词；未知动作返回 UNKNOWN_ACTION。
- 验收：用 13-6 表 10 的六个动作各准备至少三条真实用户表达做分发测试，记录模型直接命中、重试后命中、落关键词三档的比例，数据进模型测试集（对应辅导 11 模型测试集任务）。

### H5 留痕补强（实现层，时点待定，可与 H4 同批）

- 做法：调用记账增存模型原始输出原文；确认询问与决定成对记录；在留痕设计中写入两条原则（原始记录永不删除、整理操作自身留痕）。
- 验收：任一次动作执行可从留痕完整回答四个问题：模型原文是什么、程序解析成什么、用户确认了什么、最终执行了什么。

### H6（可选）场景文件按需加载

- 做法：古建 agent 的参考材料按环节拆分为场景文件，主提示词只保留目录行。属于 Agent 定义精简（辅导 11 任务）的实施选项之一，是否做由该任务统一决定。

### H7（可选）角色权限档位

- 做法：PRD 阶段讨论测绘员、审核人、项目负责人三档确认配置。第 1 代不实现。

## 6. 动作全集推导与分代建议（待 JIAJIA 确认，属于 H1 的扩展输入）

结论：动作清单不应从举例出发，应从三个来源系统推导：13-5 用户旅程的七步主线与六条支线、13-6 表 2 八种视图的主要操作列、服务端已有接口（model-runs、cad-jobs、drawing-jobs、资产下载、状态查询，来源：apps/server/src/index.ts）。按此推导出 21 个动作的全集，其中第 1 代已有 6 个，建议第 2 代新增 7 个，其余 8 个放第 3 代，另有 5 类操作明确永不给模型。完备性标准：旅程每一步和每条支线都能映射到至少一个动作或一个明确的人工节点，见 6.4 覆盖检查。

### 6.1 动作全集

| 动作 | 类别 | 旅程与界面依据 | 后端能力 | 确认级别 | 代次 |
|---|---|---|---|---|---|
| 切换视图 | 只读导航 | 表 2 八视图 | 前端已有 | 直接执行 | 1 代已有 |
| 回答问题 | 只读导航 | 全旅程 | model-runs | 只读 | 1 代已有 |
| 定位证据 | 只读导航 | 13-6 交互 5.2 双向联动，用户说"给我看 P48 的照片" | 前端联动已有，缺对话入口 | 直接执行 | 2 代 |
| 查询作业进度 | 只读导航 | 旅程第三步进度告知 | status 与作业查询接口已有 | 直接执行 | 2 代 |
| 生成修改建议 | 录入修正 | 旅程第四步、支线二；承载改参数、改类别、标记存疑不可用不可见 | model-runs | 逐条确认 | 1 代已有 |
| 框选修正 | 录入修正 | 旅程第四步、表 5 | 已有设计 | 按表 10 | 1 代已有 |
| 重新识别 | 作业 | 旅程第三步、支线二 | model-runs | 直接执行，结果标 AI 实时 | 1 代已有 |
| 录入实测记录 | 录入修正 | 旅程第二步，解析手写草图提取尺寸并关联部位 | 模型可做，落库待建 | 逐条确认 | 3 代 |
| 推进流程 | 流程协作 | 旅程第三步、交互 5.3 停靠点 | 流程状态已有 | 直接执行，停靠未清拒绝 | 1 代已有 |
| 转办待办 | 流程协作 | 旅程第四五步、支线三，存疑项转责任人 | 待建 | 确认后执行 | 3 代 |
| 降级出草图 | 流程协作 | 支线一、支线四 | 高都路径的阻断与降级已有 | 确认后执行，留痕决定人与理由 | 3 代 |
| 退回重做 | 流程协作 | 支线五，定位受影响构件局部重算 | 待建 | 确认后执行，先展示影响范围 | 3 代 |
| 变更任务参数 | 流程协作 | 支线六、任务卡视图 | 待建 | 确认后执行，先展示影响 | 3 代 |
| 解析任务书与盘点资料 | 录入修正 | 旅程第一步 | model-runs 可做，当前演示走导入包 | 结果逐条确认 | 3 代 |
| 生成三维模型 | 作业 | 旅程第六步前置 | cad-jobs 已有 | 确认后执行，展示预计耗时与用量 | 2 代 |
| 生成图纸 | 作业 | 旅程第六步 | drawing-jobs 已有 | 确认后执行，展示预计耗时 | 2 代 |
| 运行数据检查 | 作业 | 旅程第六步 | 检查链已有 | 直接执行，只读 | 2 代 |
| 导出成果 | 交付 | 旅程第七步 | 资产下载与交付 ZIP 已有 | 普通导出直接执行；交付级需证据等级满足并确认（沿用高都阻断规则） | 2 代 |
| 起草交付说明 | 交付 | 旅程第七步 | model-runs 可做 | 生成草稿，逐条确认 | 2 代 |
| 设置图纸样式 | 交付 | 图纸样式视图 | 样式模板部分已有 | 逐条确认 | 3 代（可选） |
| 存入构件库 | 知识沉淀 | 旅程第五步，判断结论入库复用 | 构件库复用已有 | 责任人确认 | 3 代 |

### 6.2 定义一次到位，实现分批，暴露只含已实现

结论：21 个动作在设计层一次全部定义完（H1 修订时全部入表 10，每个都补齐八字段），实现分两批，模型可见目录任何时刻只包含已实现动作。

这个拆法对照了 dsh 的实际做法（来源：docs/tool-catalog.zh.md 与 docs/subsystems/skills.zh.md）：dsh 全目录有 45 个以上模型可见工具，全部按同一契约定义，但没有任何部署全暴露；每个部署通过插件组合与工具限制裁剪出自己的子集，个别插件出厂即标记禁用，启用需显式选择。它分的是暴露，不分定义。古建照此办理：定义不分代（避免表 10 反复返工，也让确认级别和留痕设计一次想全），实现按就绪度分批。

实现批次的划分依据是项目内约束，与 dsh 无关：第 2 代 7 个（序 3、4、15 至 19）后端能力已存在或模型即可完成，属于主线演示闭环；第 3 代 8 个需要新建后端能力或协作机制，按辅导 11 的顺序在 PRD 与架构确认后实现。第 2 代上线时模型可见目录共 13 个动作；第 3 代动作在表中标注未实现，不进模型目录，避免模型调用注定失败的动作。

### 6.3 永不给模型的操作

以下操作不进任何一代动作清单，只保留人工入口，理由是专业责任或留痕完整性：

1. 正式签发。表 6 停靠点已规定只有责任人可操作。
2. 删除项目或清空数据库。
3. 修改留痕记录与确认记录。留痕只追加，与第 3.2 节原则一致。
4. 修改原始证据文件。
5. 指定或变更责任人。任务卡人工操作。

### 6.4 覆盖检查

| 旅程环节 | 承接动作或人工节点 |
|---|---|
| 第一步 说清楚要做什么 | 动作 14（3 代）；当前以导入包承接 |
| 第二步 补齐资料 | 动作 8（3 代）、动作 5 |
| 第三步 系统自己往下做 | 动作 7、9、4 |
| 第四步 圈一下就能改 | 动作 6、5 |
| 第五步 专业判断交给负责人 | 动作 10、21（3 代）；判断本身是人工节点 |
| 第六步 出图和检查 | 动作 15、16、17、5 |
| 第七步 签发和交付 | 动作 19、18；签发是人工节点 |
| 支线一 资料不够 | 动作 11（3 代）；阻断由前置条件承担 |
| 支线二 识别错了 | 动作 6、5、7 |
| 支线三 专业说不准 | 动作 10（3 代） |
| 支线四 无实测基准 | 前置条件阻断，非动作 |
| 支线五 审核退回 | 动作 12（3 代） |
| 支线六 中途改要求 | 动作 13（3 代） |

### 6.5 补充判断

1. 修改参数不单设动作。用户说"把柱径改成 300"路由到动作 5 生成修改建议，程序合理性核对后逐条生效。绕过该流程的直接写入会破坏已通过古建专业审查的留痕设计。
2. 高成本动作（15、16）两条通用规则：执行前展示预估（生成作业实测约 4 分钟量级，来源：执行会话 2026-08-14 运行记录）；执行中可取消。作业失败将结构化原因回给模型组织说明，不自动重试。
3. 目录保持稳定：13 个动作全程对模型可见，环节切换不增删目录，不可用时由前置条件拒绝并说明原因。这是 harness 计划模式的做法（进出模式不改工具目录），避免模型因目录变化而路由混乱。
4. 13 个动作的路由准确率是一个风险，标注为假设：辅导 11 提示词做减法的原则同样适用于动作描述，每个动作描述控制在两句内。验证方式即 H4 验收的分发测试，测试集需覆盖全部 13 个动作各至少三条真实表达，并加入 5 条应落到"回答问题"的边界表达。
5. 分工边界不因动作扩展移动：AI 触发作业、组织说明、提出修改；实测录入的最终确认、专业存疑判断、签发仍由人执行。写进 13-2 分工表述即可。

## 7. 面试可用的一句话

对照业界开源 harness 的工具定义与审批设计，为产品的 AI 动作清单补齐了参数契约、失败行为和确认异常默认值三类字段，并说明了哪些平台机制因为产品是垂直单场景而明确不采用。这句话的每个成分都有本文与 13-6 修订记录作证据。
