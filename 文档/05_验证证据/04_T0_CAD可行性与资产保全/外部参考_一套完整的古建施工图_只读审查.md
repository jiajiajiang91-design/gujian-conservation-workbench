# 外部参考《一套完整的古建施工图》只读审查

## 1. 审查边界

- 审查日期：2026-08-11
- 源文件：`D:\Downloads\一套完整的古建施工图.dwg`
- SHA-256：`755DA8B23AF00A29829C76E65772FA51BD3D9F3524641ECEA99103D1C5EE88B1`
- 哈希复核：`Get-FileHash -LiteralPath <源文件> -Algorithm SHA256` 与 `certutil -hashfile <源文件> SHA256` 独立重复计算，结果均为上述 `755D...88B1`；复核时文件长度为 6,716,072 字节，最后写入时间为 2026-08-11 12:14:37（BST）。
- 只读工具：PowerShell `Get-FileHash`；AutoCAD 2024 Core Console `U.61.0.0`
- 审查方式：直接读取文件头、表记录和顶层对象统计；未保存 DWG，未导出图元、图片或文字内容。

早期审查消息曾误报 SHA-256 为 `DFE9F7811AA65577D9AC4C53267267F4F226D1025E591955A3AD941BACE82AD9`。工作区没有与该值对应的文件或工具日志，无法建立其证据来源；该值属于错误记录，现已由当前源文件的两次独立哈希计算纠正为 `755DA8B23AF00A29829C76E65772FA51BD3D9F3524641ECEA99103D1C5EE88B1`。在没有文件变化证据的情况下，不将这一差异解释为源文件被替换。

本文件没有随附作者、许可、测绘依据或版本说明。本审查只判断它能提供哪些视觉参照，不判断其设计正确性、来源真实性和复用权利。

## 2. 文件事实

### 2.1 图种与组织

标题类文字的匿名关键词统计表明，文件包含平面、立面、剖面、详图和表格类内容。关键词命中只证明图种存在，不等于独立图纸数量，也不证明各图同源或尺寸正确。

文件有三个布局记录，其中一个是模型空间，两个纸空间布局没有顶层对象。125,060 个顶层对象全部位于模型空间，纸空间对象和视口均为 0。因此该文件没有用纸空间布局与视口组织成套图纸；可见图框和图形分组只能位于模型空间或块定义中。

### 2.2 单位、图层与对象

- `INSUNITS=0`，文件没有声明可直接采用的模型单位。
- 共 29 个图层，其中 28 个被顶层对象使用。为避免形成复制依赖，本记录不保存图层名称。
- 共 13 个命名块定义、69 个块参照。块数量只能说明存在复用组织，不能证明块具有稳定构件语义。
- 顶层对象以线、圆弧、多段线、圆和样条为主；另有 138 个填充、858 个单行文字和 21 个多行文字。
- 原生 `DIMENSION`、`LEADER`、`MLEADER`、`VIEWPORT` 均为 0。文件中的可见尺寸和引注不能据此认定为可编辑的原生标注对象。
- 未发现顶层图像对象和代理对象。该结果只覆盖本次顶层只读统计，不替代完整依赖审计。

上述数量只用于描述文件结构，不作为本项目的质量门槛。对象多、文件大或线条密集都不能证明图纸专业性。

### 2.3 布局与字体

纸空间布局没有承载图框、视口和图纸对象，不能作为本项目 A1 布局的实现样板。

文件包含 5 个文字样式和 5 项字体引用。AutoCAD 打开文件时报告至少一项中文字体被替换为通用 SHX 字体。字体文件、再分发许可和跨软件一致性没有闭合，因此该文件不能作为本项目字体、文字样式或 PDF 输出的金标准。

## 3. 可以借鉴的视觉维度

该文件只能用于人工检查以下内容：

- 平面、立面、剖面、详图和表格是否成组出现；
- 主图、详图、尺寸、标高、索引和说明的页面层级是否清楚；
- 构件密集区域是否有放大详图和局部说明；
- 图纸是否形成从整体关系到局部构造的阅读顺序；
- 线条、填充、文字和留白的总体密度是否接近工程图使用场景。

这些观察只能进入人工成组预览清单，不能转成外部文件驱动的坐标、尺寸、图层、块或图元数量门槛。

## 4. 不能作为金标准的内容

该文件不能作为以下内容的依据：

- 几何真源、现场测量值、构件尺寸、标高或材料事实；
- 古建形制、屋面曲线、构件关系和节点做法的正确答案；
- 原生尺寸、引线、布局、视口和打印闭包的合格样本；
- 本项目的图层名、块定义、填充纹样、文字、图签和字体来源；
- `sourceEntityId`、几何修订、视图修订和成果版本之间的追溯关系；
- 版权许可、正式设计责任或保护工程适用性。

本项目不得复制该文件的图元、尺寸、图层、块、填充、图签、文字、字体配置和打印样式，也不得以重新描摹、哈希绑定或路径引用的方式间接使用。

## 5. 对 13-12 质量基准的影响

该参照强化了 13-12 中“成套图种和专业阅读层级”的人工要求，但同时暴露出以下不能照搬的做法：

- 13-12 要求模型空间 1:1 且单位明确；该文件单位未声明。
- 13-12 要求纸空间图幅、图签和比例视口闭合；该文件的纸空间布局为空。
- 13-12 要求原生 `DIMENSION`、`TEXT/MTEXT`、`HATCH`、`INSERT`、`LAYOUT` 和 `VIEWPORT`；该文件不能证明原生尺寸、引线和视口合格。
- 13-12 要求中文无缺字、无替换且交付字体许可闭合；该文件打开时发生字体替换。
- 13-12 要求来源追踪和修订闭包；该文件没有可用于本项目验证的来源关系。

因此，该文件只能作为“视觉完整度参照”，不能作为 L1、原生 CAD 或同源几何的验收答案。

## 6. 对当前 DrawingPackageContract 的影响

当前独立 DrawingPackageContract 的以下边界必须保留：

1. 生成输入白名单只允许 DrawingPackageContract、已绑定的 ViewGeometry、几何清单和团队字体。
2. 外部 DWG、外部哈希、外部图层、外部块、外部尺寸、外部图签、外部文字和外部填充不得进入生成依赖。
3. 规范 DXF 必须使用毫米、模型空间 1:1、两个 A1 纸空间布局和锁定视口，不能复现该参照的模型空间排版方式。
4. 尺寸、标高、引注和详图索引必须来自项目合同与已绑定来源，并以原生 CAD 对象表达；不得依据该参照补值。
5. 字体必须绑定团队可再分发资源，PDF 字体嵌入，跨软件检查为零缺字、零替换、零问号。
6. 外部文件移除后，DXF、SVG、PDF、预览图和追溯侧车文件仍须完整生成并通过反向解析。
7. 外部文件名、路径和哈希注入生成输入或成果时，验证必须失败。

当前合同仍应保持 `generated-not-qualified`、`L1=false`。本参照不能关闭简化承托构造、团队字体未绑定、跨 CAD 往返未完成和专业成组预览未完成等阻断项。

## 7. 结论

该文件适合作为图种组合、页面层级和信息密度的只读视觉参照，不适合作为几何、尺寸、许可、原生 CAD、字体或来源追踪的金标准。项目实现必须继续使用自有 ViewGeometry、独立 DrawingPackageContract 和团队字体，并通过反向解析与第二 CAD 往返证明成果闭包。
