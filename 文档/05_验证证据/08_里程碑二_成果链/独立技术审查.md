# 里程碑二独立技术审查

> 审查角色：未参与实现的独立技术审查角色
> 初审冻结提交：`f375960`
> 第一轮 P0 修复提交：`0e81be0`
> 最小关闭提交：`0fa42f2`
> 最终结论：`P0=0`
> 资格边界：`generated-not-qualified / L1=false / 代理成果 / 未签发 / 不可用于正式交付或施工`

## 1. 审查边界

初审检查里程碑二的技术闭包、动态成果矩阵、几何与制图来源、项目包回导、CAD 兼容验证、进程退出和 T0b 资产保护。P0 修复后的核对严格限制为两项：

1. `ArtifactRequirementMatrix` 是否进入唯一命令写入口、IndexedDB、共享闭包及 JSON/ZIP 项目包，并能在空库回导后继续生成新版本。
2. 当前 AutoCAD/QCAD 摘要是否绑定当前规范 DXF、布局、未保存回写边界及制图验证报告。

本次没有重新扩展审查范围。原 P1/P2 继续作为待办，不阻断本里程碑技术门槛。

## 2. 初审结论

初审确认以下能力已经成立：

- GeometryRevision、Artifact、CheckRun、DeliveryEvaluation、DeliveryDraft 已有同版本闭包校验。
- 图种、图幅、比例、布局来自结构化任务要求；当前节点详图有独立剖切面、局部裁剪和独立比例。
- 当前几何的 BREP、GLB、IFC、manifest、来源映射、报告和预览绑定同一 GeometryRevision。
- 剖面使用 OCP/BREP 真实求交；可见线采用独立遮挡区间算法，当前成果可复算。
- 当前 DXF 包含原生 LINE、DIMENSION、TEXT、MTEXT、HATCH、INSERT、LAYOUT 与锁定 VIEWPORT。
- T0b 迁移表为 81 行；恢复快照核心文件哈希保持原值。

初审发现的两个最终 P0 是：

1. `ArtifactRequirementMatrix` 仅为浏览器临时对象，项目包回导后成果引用无法解析到矩阵实体。
2. AutoCAD/QCAD 摘要字段与验证器不一致，当前制图验证报告没有绑定两个摘要的 SHA-256。

## 3. P0-1：成果矩阵与项目包闭包

状态：**已关闭**。

### 实现证据

- `CommitArtifactSet` 和 `ImportProjectSnapshot` 接收并校验 `artifactRequirementMatrices`：
  - `packages/application/src/commands.ts`
  - `packages/application/src/project-command-service.ts`
- 共享闭包拒绝缺失矩阵和跨 GeometryRevision 的矩阵引用：
  - `packages/application/src/delivery-chain-closure.ts`
  - `packages/application/src/delivery-chain-closure.test.ts`
- IndexedDB 升级为 v6，增加 `artifactRequirementMatrices` 对象库、事务读写和审计 write-set：
  - `packages/infrastructure/src/indexeddb-project-repository.ts`
- ProjectData JSON/ZIP 导出和导入包含矩阵：
  - `packages/infrastructure/src/project-package-service.ts`
- DrawingJobClient 通过唯一命令将当前矩阵与图纸成果一起提交：
  - `apps/workbench/src/drawing-job-client.ts`

### 回导验证

`packages/infrastructure/src/project-package-service.test.ts` 覆盖：

- JSON 空库回导后矩阵记录存在，成果 `requirementMatrixId` 可解析；二进制诚实标记缺失。
- ZIP 空库回导后矩阵、真实二进制、几何、成果、检查、交付和审计记录恢复。
- 回导后继续建立新的 GeometryRevision、新矩阵、图纸成果、CheckRun、DeliveryEvaluation 和 DeliveryDraft。

复核运行 `pnpm run test`：根级 41 项、服务端 10 项、工作台 10 项测试全部通过。

## 4. P0-2：AutoCAD/QCAD 与当前 DXF 绑定

状态：**已关闭**。

### 验证器闭包

`workers/cad/project_drawings/verify_package.py` 已适配当前摘要 schema，并校验：

- AutoCAD 使用当前 canonical DXF 哈希；审计前后副本与 canonical DXF 一致；零错误、零修复、零删除；字体未替换；canonical DXF 未修改。
- QCAD 输入和检查后副本均绑定当前 canonical DXF；打开和打印成功；打印布局与任务矩阵一致；未保存回写，未修改 canonical DXF。
- 两份摘要文件自身 SHA-256 写入制图验证报告。

### 当前成果复算

- 当前 DXF SHA-256：`be53818ff16a41ec8712b26981144bc3be639614ba0e51077644df9c688217b8`
- AutoCAD 摘要 SHA-256：`253f2f5a0d6cc69c5df0b21de4b3f9c53eb27e5d66f6be9e98a4f8d9b3509f5a`
- QCAD 摘要 SHA-256：`be3dde5ea997f3515c81715e500bc6f3383c2000c804e470810f1239559e088f`
- `habs-current-drawing-verification.json`：`23/23` 通过，`failedCheckCount=0`，两个摘要 SHA-256 均非空且与文件实算一致。
- QCAD 打印布局：`P-01`、`P-02`；`saveBackPerformed=false`；`canonicalDxfModified=false`。

复核直接调用当前验证器，结果为：

```text
status=passed-task-driven-drawing-artifacts
checkCount=23
failedCheckCount=0
```

`workers.cad.project_drawings.test_verify_package` 的 5 项测试全部通过，包含摘要哈希攻击负例。

## 5. 回归与退出

- `pnpm run test`：通过。
- 当前制图验证器：23/23 通过。
- Python 制图验证器测试：5/5 通过。
- 上一轮完整 `pnpm run check`、production build 与几何/制图测试记录保持通过。
- 本次复核结束后，5173 和 8787 均无监听端口。

测试数量只证明实现回归，不作为古建专业质量证明。

## 6. 未关闭待办

### P1

1. Node 服务关闭时尚未统一取消并等待所有在途 Kimi、几何和制图子进程；应在 T13 的统一启动/退出脚本和进程攻击测试中关闭。
2. 非剖面可见线基于 GLB 三角网格候选边和独立遮挡区间算法。当前多面体成果通过，但曲面 BREP 的精确 HLR 仍需后续能力验证。

### P2

1. `completedDrawingJobs` 尚无容量和生命周期上限。
2. IndexedDB 的 `modelRunEvents` 对象库尚未形成独立读写闭包。

以上 P1/P2 不改变当前所有成果的代理与未签发边界。

## 7. 最终结论

两项技术 P0 均已按最小范围关闭，最终为：

```text
P0 = 0
P1 = 2
P2 = 2
```

从独立技术审查角度，**允许进入 T11**。进入条件仍包括：独立古建专业审查 `P0=0`，以及里程碑二最终验收证据提交完成。本结论不授予专业资格、L1 资格或正式签发能力。
