# 公开仓库发布 · 验收

条件逐条可判定。做不到即为未完成。

## 一、写定时点的说明

本单元的扫描（12.3 第 3 步）在验收条件写定之前已执行，因为委托里把跑扫描排在建单元之前。扫描属只读操作，不改远端也不改工作区。

两个对外动作（第 1 步建备份、第 4 步替换 `main`）的条件在执行前写定，符合验收条件开工前写定的口径。

## 二、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 扫描报告产出，逐项覆盖密钥、用户名、绝对路径、私有资料、许可、外部 DWG、大对象、过期资格表述八项 | 通过 |
| 2 | 扫描报告中的必须处理项全部处置完毕，每项写明处置方式 | |
| 3 | 体量取舍已定，`12.3` 第 2 步的目标与实际一致，不留一个达不到又不改的指标 | |
| 4 | 远端存在 `archive/portfolio-v0.0` 分支与 `portfolio/v0.0` 标签，两者精确指向 `ac6abbc` | |
| 5 | 备份建立在任何其他远端写操作之前 | |
| 6 | `main` 的内容与本地工作区一致，且不含实施分支的提交历史 | |
| 7 | 发布后的 `main` 上不存在扫描第二节列出的任何一项 | |
| 8 | README 写明开发周期与关键节点，替代提交历史承担的时间线说明 | |
| 9 | 从公开仓库全新克隆后，`corepack pnpm install && corepack pnpm check` 退出码 0 | |
| 10 | 回退演练：确认从 `archive/portfolio-v0.0` 能恢复到 `ac6abbc` 的内容 | |

## 三、判定说明

第 5 条不能事后补证。备份必须先于替换，顺序错了即便结果看起来一样，中间那段时间是没有回退点的。

第 7 条要在发布后的远端上重新验，不能用本地扫描结果代替：替换过程本身可能带入未跟踪或新生成的文件。

第 9 条是发布是否可用的唯一硬判据。仓库能打开不等于别人能跑起来。

## 四、证据

扫描报告：`../../05_验证证据/13_发布前扫描/发布前扫描报告.md`。

发布后的克隆验证记录写入同目录。

## 五、结论

未完成。第 1 条通过，其余九条待执行。第 2、3 条要你先定处置与取舍，第 4 至 10 条要你确认后才执行。
