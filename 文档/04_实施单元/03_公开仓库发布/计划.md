# 公开仓库发布 · 计划

按组执行，末组为验证。对外动作单独成组，执行前停下等确认。

## 第一组　扫描（已完成）

1. 逐项扫描跟踪文件：密钥、用户名、绝对路径、私有资料、许可、外部 DWG、大对象、过期资格表述。
2. 报告写入 `../../05_验证证据/13_发布前扫描/发布前扫描报告.md`。

结论：不可发布，五项必须处理，体量超目标近一倍。

## 第二组　处置扫描发现（待你定后执行）

三项处置方式明确，两项要你先定。

| 序 | 事项 | 处置 | 是否需你定 |
|---:|---|---|---|
| 1 | 仓库无 LICENSE | 选一个许可加进根目录 | 要定用哪个 |
| 2 | `项目目录说明.md:20` 暴露本机目录与个人作品集路径 | 删去该句，或改为不含具体路径的表述 | 否 |
| 3 | 三个 AutoCAD 日志含 Windows 用户名（7 处） | 用户名段替换为占位，文件头注明替换过什么，其余原样 | 否 |
| 4 | Dai Loy HABS 目录未写许可 | README 补一节写明 HABS 为美国联邦政府作品、属公有领域、来源页与获取时间 | 否 |
| 5 | 无根 README | 按第五组一并写，发布前就位 | 否 |

第 1 项的选择影响别人能不能用这份代码。作品集场景常见两种取向：一是宽松许可（如 MIT），表示欢迎参考与复用；二是只授予查看权（无许可即默认如此，但应写明而不是留空）。这一项由你定。

## 第三组　体量取舍（待你定）

扫描报告第四节给了三条路，建议丙：不减，把 12.3 的 50 MB 目标改为 100 MB 以内并写明理由。

选甲要删上游可变字体，克隆者跑不了字体闭包校验。选乙要改演示项目定义，演示少两类成果，须先改 `01_产品/04_演示项目定义.md`，不在本单元内绕过。

定下之后，若目标数字变化，同步改 `02_技术/01_技术架构.md` 12.3 第 2 步。

## 第四组　建备份（对外动作，执行前停）

1. 确认远端 `ac6abbc` 仍在且内容为 2026-08-12 的作品集版本 77 个文件。
2. 建 `archive/portfolio-v0.0` 分支与 `portfolio/v0.0` 标签，两者精确指向 `ac6abbc`。
3. 推送两者，核对远端上确实存在且指向正确。

这一步必须先于第五组。它是唯一的回退点。

## 第五组　替换（不可逆，执行前停）

1. 用当前工作区状态建立全新初始提交，不携带实施分支历史。
2. 写根 README：开发周期、关键节点、这是什么、怎么跑起来、边界（代理成果，未签发，不构成专业结论）。
3. 推送替换 `main`。

## 第六组　验证

1. 在远端重新扫描一遍第二节各项，确认发布后的 `main` 上一项不剩。
2. 全新克隆到临时目录，跑 `corepack pnpm install && corepack pnpm check`，退出码 0。
3. 回退演练：确认从 `archive/portfolio-v0.0` 能恢复 `ac6abbc` 的内容。
4. 记录写入 `../../05_验证证据/13_发布前扫描/`。
5. 回填验收.md。
