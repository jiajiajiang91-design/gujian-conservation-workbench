# 框选修正接入 · 验收

条件在开工前写定，逐条可判定。做不到即为未完成，不以能跑通或已接线代替。

## 一、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 在证据区照片上拖出选框后，输入区出现位置引用，可移除 | 通过（无截图，见实测记录第二节） |
| 2 | 框选后输入一句话发送，构件表新增对应行，来源标记为人工确认 | |
| 3 | 该次修正出现在修改历史中，含理由、操作人、时间 | |
| 4 | 五类说明方式各执行一次并达到预期：新增、类别修改、位置调整、删除、标记不可见 | |
| 5 | 删除类修正进入排除记录，重新识别后该项不再出现 | |
| 6 | `client-op-adapter.ts` 中不再存在返回尚未接入的分支 | 通过 |
| 7 | bbox 无法关联构件时界面向用户提问，不静默失败 | |
| 8 | 代码登记表中 `marquee_correction` 状态为 `executable`，界面文档表 10 同步，`action-catalog.test.ts` 通过 | 通过 |
| 9 | 分发用例集三条框选用例执行完毕，结果回填用例集第 3 节 | |
| 10 | `npm run typecheck` 退出码 0，`npm run test` 全绿 | 通过 |

## 二、待判定的三处差异

实现与文档不一致的三处，不自行判定通过，交 JIAJIA 决定改文档还是改实现。

| 序 | 文档写的 | 实现做的 | 成因 |
|---:|---|---|---|
| 1 | PRD F11 验收标准为框选加一句话即可完成修正；界面文档表 10 写新增和遮挡标记直接执行并留痕 | 生成带位置引用的修改建议，逐条确认后才写入 | 与助手建议走同一条确认通道 |
| 2 | 界面文档表 6 写这里没有构件即删除该条并记入排除记录 | 提示构件位置无来源，请改用构件编号或构件清单删除 | 识别记录不含构件 bbox |
| 3 | 界面文档表 6 写框位置不对即调整位置框并重算相邻关系 | 同上，依赖构件命中，当前走提问 | 同上 |

第 2 与第 3 条同源：三个演示项目的识别记录只有构件清单与尺寸，没有图像空间位置。要让这两类说明方式成立，需要识别环节产出构件 bbox，属于新增能力，不在本单元范围内。

## 三、判定说明

第 8 条是本单元的防虚报条件。此前该动作在 PRD、界面文档与产品状态入口三处记为已实现，而前端只有桩，原因是文档可以单方面宣称交付。现在实现状态由代码登记表派生并有测试守着，文档与代码不一致会在测试中失败。

第 9 条不可跳过。这三条用例自动作层验收起一直标为待框选界面接入后补测，本单元完成即失去延后理由。

## 四、证据

截图与执行记录写入 `../../05_验证证据/10_动作层验证/`。

已回填：第 1、6、8、10 条见 `框选交互实测记录.md`。该记录第二节写明本次没有截图，浏览器面板未显示，页面不合成帧，以 DOM 读数代替；指针捕获未验证。

未回填：第 2、3、4、5、7、9 条都要真实走一次助手回合，会发起模型调用，按既定规矩先报再跑。

## 五、结论

第一节十条通过且第二节三处差异处置完毕后，由 JIAJIA 确认本单元完成。
