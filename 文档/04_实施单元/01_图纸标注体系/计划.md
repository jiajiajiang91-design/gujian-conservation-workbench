# 图纸标注体系 · 计划

按组执行，末组为验证。已完成项标注提交号。

## 第一组　标注来源统一

1. 标注对象收进 Drawing IR，四个渲染器改为只读 IR，不各自推算。已完成，提交 9b5e347
2. 标注策略与规划从项目事实派生，轴网与标高不写死在生成器分支。已完成，提交 4efc75f

## 第二组　轴网与尺寸链（方案 A）

3. 从项目事实取柱心线或墙心线生成定位轴线与轴号，每条带依据说明。已完成，提交 4a6d5a7
4. 由轴网位置派生开间进深与总尺寸三级链。已完成，同上
5. 轴号圈成图间距不足 8 mm 的整条丢弃并计数，不压线不缩圈。已完成

## 第三组　标高与索引（方案 B）

6. 竖向标高标记，取值优先用项目写明值，几何值兜底。已完成
7. 剖切符号与详图索引符号。均已完成。详图索引初次判定为演示项目无对应关系，核对后不成立，实为匹配规则要求剖切偏移完全相等，改按法向认亲与偏移就近匹配后正向索引与回引都产出
8. 项目写明值与几何值差异超一个构造层厚度时并列记入 `levelConflicts`，不由制图侧选值。已完成

## 第四组　构件引线（方案 C）

9. 按 R3 词表首选名对关键构件出引线标注。已完成
10. 标注避让：八个方向均无法避开已有标注的单独计数并写入产物。已完成

## 第五组　验证

11. 三个演示项目重出图纸，`node tools/check-demo-matrix.mjs` 二十一格仍全满
12. 逐格点开确认标注内容成立，不只看条数
13. 量化对照写入 `../../05_验证证据/12_图纸差距与一致性核对/标注体系补齐前后对照.md`。已完成
14. 按验收.md 逐条判定并填写结果列

## 完成定义

- 验收.md 八条全部通过
- 三个演示项目重出图纸后 `node tools/check-demo-matrix.mjs` 二十一格全满
- 跑 `node .claude/skills/quality-check/scripts/run-all.mjs --full`，必须修复项为 0
