# T0a / T0b 资产保全与迁移建议

> 日期：2026-08-13
>
> 分支：`codex/professional-workbench-rebuild`
>
> 范围：只完成 T0a 和 T0b，不进入 T1 或代码实施

## 1. 执行结论

T0a 和 T0b 已完成。

- PRD v1.1、技术架构 v1.2、当前分支、CAD 工具责任和资产边界已写入权威状态文档。
- 81 项未提交资产已建立本地恢复包，包括 24 项 Git 跟踪变化和 57 项未跟踪文件。
- 恢复包已在全新临时 worktree 中实际恢复。81 项状态一致，76 个存在文件的 SHA-256 全部一致，5 个删除状态全部一致。
- 资产只完成保全与分类，没有将旧 v2/v3 输出重新定义为合格成果。CAD 与三维资产仍为 `generated-not-qualified`、`L1=false`。

## 2. T0a 结果

T0a 完成了三项权威依据同步。

| 文档 | 结果 |
|------|------|
| `.claude/context/pm-context.md` | 改为 PRD v1.1、架构 v1.2、当前分支和 T0a/T0b 暂停门槛 |
| `13-9_技术架构_古建保护成果工作台.md` | 标记 v1.2 已确认，引用 `13-12` v0.4 |
| `13-12_专业制图与建模质量基准.md` | 升级为 v0.4，明确 AutoCAD 资格验收与第二 CAD 兼容性检查的分工 |

T0a 使用两个独立提交：

1. `fd9ddba` `docs(context): record approved architecture state`
2. `7b7b96c` `docs(context): align quality baseline version`

## 3. T0b 恢复包

恢复目录：`D:\Claude_jiajia\06_古建保护归档\归档\recovery\2026-08-13-pre-t0b`

该目录被 `.gitignore` 排除，不会被普通 Git 提交误收。

| 文件 | SHA-256 | 用途 |
|------|--------|------|
| `tracked-working-tree.patch` | `f4693d3cdccd3cb37857b6429b39d117f1b20dba4be601096dbccb4968d5dd9e` | 24 项跟踪变化的 Git binary patch |
| `staged-index.patch` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | 空索引补丁，证明快照时暂存区为空 |
| `untracked-files-v2.zip` | `85c5794ffe967ca3a3bec1b5eaaf0699b92b4eccc69ffd192459c680685c8a6f` | 57 项未跟踪文件，UTF-8 路径 |
| `asset-inventory.json` | `3d761da3ddf60bf7ca3184f59577b36ad1871a4025c60ade79e28bdf64336f21` | 81 项文件级 SHA-256 清单 |
| `restore-byte-policy-v2.json` | `0ea57fe3129217a42ab72aa2bee8dcef29d92993591d67c79cfbc6aa9466cad1` | 4 个 CRLF 跟踪产物的字节恢复规则 |
| `restore-verification-v2.json` | `ee739944e77828c8ba9fde6faf0b81257ec5f0b45aef019eee830470d167dd3e` | 实际恢复验证结果 |
| `snapshot-manifest.json` | `6b255f0c5be16e133400eda9ecc6973726792771edbaf75d918125d60285b137` | 恢复入口与文件闭包 |

### 3.1 编码问题处理

第一版 ZIP 的中文条目名发生错误转码。该尝试已标记为无效，保留文件名为 `untracked-files-v1-invalid-path-encoding.zip`，禁止用于恢复。

第二版已核对：

- ZIP 条目 57 项，与未跟踪清单完全一致。
- 正确中文路径 36 项。
- 错误转码路径 0 项。
- 全部条目在解压前检查绝对路径和路径穿越。

## 4. 恢复验证

验证在全新的 detached worktree 中执行，基准提交为 `fd9ddba02f6aaf9263cff41cd5d6293d1b59b59f`。

| 检查 | 结果 |
|------|------|
| 应恢复资产 | 81 |
| 已恢复存在文件 | 76 / 76 |
| 已恢复删除状态 | 5 / 5 |
| SHA-256 不一致 | 0 |
| Git 状态条目 | 81 |
| 中文目录 | 正确 |
| 错误转码目录 | 0 |
| 临时 worktree | 已清理 |

Git 对 4 个跟踪文本产物的补丁应用会将 CRLF 规范化为 LF。恢复包因此固定了字节级 LF→CRLF 步骤，实际复原后四文件的尺寸和 SHA-256 均与快照一致。

## 5. 资产清单与分类

机器可读清单为 `验证材料/07_T0b资产保全/t0b-classified-asset-inventory.json`，共 81 项、160,585,881 字节，约 153.1 MiB。

| 资产组 | 数量 | 容量 | 分类 | 建议 |
|------|------:|------:|------|------|
| 根级领域与基础设施 | 5 | 3,143 B | 修正后迁移 | 等 T2/T3 补齐锁文件、`src`、迁移与测试后提交 |
| v2 代码与测试 | 13 | 341,186 B | 修正后迁移 | 保留 IR、DXF、跨格式、来源映射和攻击验证；删除固定视图、布局、标题和日期 |
| v2 字体闭包 | 5 | 10,614,907 B | 修正后迁移 | 脚本、语料、manifest 和测试进 Git；可重建 TTF 进 Release 或忽略目录 |
| v3 源码与 fixture | 10 | 252,299 B | 修正后迁移 | T8a 保全 demo，T8b 抽取无固定形制的 `GeometrySpec` 内核 |
| v2 IR 与合同输出 | 4 | 8,564,585 B | 失败或历史证据 | 绑定旧 v2 revision 和固定布局，只作迁移参考 |
| v2 原生 DXF 与审计 | 9 | 62,713,580 B | 失败或历史证据 | 不属于 v3 同源链；先恢复 5 个被删的小型审计证据，未经确认不删除 |
| v2 SVG/PDF/PNG 成组输出 | 18 | 44,624,612 B | 失败或历史证据 | 专业复核已拒绝，保留 `rejected`，不作合格作品集成果 |
| v3 生成输出 | 17 | 33,471,569 B | 失败或历史证据 | `geometry` 为旧 881/24；`prefreeze` 为 886/2118 但验证哈希过期；分别标记 `superseded` 或 `invalidated` |

分类汇总：

- 修正后迁移：33 项，11,211,535 字节。
- 失败或历史证据保留：48 项，149,374,346 字节。
- 直接保留的已提交资产：`b03ac05` 中的 Google Fonts 官方 Noto Sans SC、`OFL.txt`、`METADATA.pb`，以及已确认的 PRD/architecture/T0a 文档。
- 不进主线：依赖、缓存、staging、可重建大产物和本地恢复包。

## 6. 安全与公开仓库边界

已对候选资产执行路径级扫描。

- 未发现 `.env`、私钥、PEM/P12/PFX 或常见云服务凭据文件。
- 大小写精确的 OpenAI/AWS/Google/私钥特征扫描无命中。
- 绝对路径命中均位于攻击性负例，用于证明生成器拒绝 `D:\Downloads`、`C:\Users`等外部路径，不是生成依赖。
- 当前约 37 MB DXF、25.6 MB sidecar、SVG、PNG、GLB、IR 和派生字体不建议进公开仓库主线。它们应放入 GitHub Release 或本地忽略目录，由小型 manifest 记录哈希、版本和资格边界。

## 7. 拟提交拆分

本次 T0b 只提交分类清单和本报告，不提交 81 项原始脏资产。后续建议按以下边界执行，每项均等待 JIAJIA 确认。

| 顺序 | 拟提交 | 内容 | 暂不纳入 |
|------:|--------|------|------------|
| 1 | `docs(audit): restore invalidated native cad evidence` | 恢复 5 个被删的 AutoCAD/QCAD 小型审计记录，绑定旧 DXF 哈希和失败原因 | DXF、sidecar 大文件 |
| 2 | `feat(font): add reproducible licensed font derivation` | 字体派生脚本、语料、manifest 和测试 | 可重建 TTF |
| 3 | `test(cad): preserve v2 drawing pipeline assets` | v2 IR、DXF、跨格式和验证代码/测试，先作迁移资产提交 | 固定成果输出和合格宣称 |
| 4 | `feat(cad): preserve v3 geometry demo baseline` | v3 源码、fixture、独立验证与攻击测试，明确 `generated-not-qualified` | 过期验证报告和大型输出 |
| 5 | `docs(audit): retain rejected and superseded prototype evidence` | 小型 build record、verification、review record，统一标记 `rejected`、`invalidated`、`superseded` | 大型可重建产物 |
| 6 | GitHub Release | DXF、SVG、PDF、PNG、GLB、IR、sidecar、派生字体，附 SHA-256 manifest | Git 主线 |

根级领域与基础设施半成品不建议现在独立提交。它们应在 T2/T3 补齐源码、锁文件、迁移和测试后再提交。

## 8. 暂停门槛

T0a 和 T0b 完成后，项目在此暂停。未经 JIAJIA 确认，不执行 T1，不整理或删除 81 项脏资产，不将大产物推送到 GitHub，不开始新的代码实施。
