# V1A 数据基础开发验收记录

> 日期：2026-08-09  
> 对应 PRD：`13-8_核心PRD_V1A正式立面成果闭环.md`  
> 对应架构：`13-9_V1A技术架构与开发任务.md`  
> 验收范围：D1 至 D5，不包含正式 CAD/DXF 生成和 V1A 正式成果验收

## 1. 结论

D1 至 D5 已实施并通过自动测试与本地浏览器冒烟验证：项目数据已从界面代码外置；高都与东呈是两个可真实切换的数据集；项目状态按 ID 隔离；导入、导出和同 ID 副本可用；资源路径、来源、位置框和恶意字段受到校验。

V1A 仍未完成。两个内置项目均为 `demo-proxy`，没有一套带可核查原始实测证据的数据能够进入正式交付。静态 PNG 只作为既有成果预览，不能代表当前数据实时生成的正式图。

## 2. 独立复核

本轮在编码前后使用独立 solution architect 角色检查架构。初次复核给出 6 个阻断问题：实测来源矛盾、导入安全、项目硬编码、异步切换污染、静态正式图版本、导出包覆盖原始证据。

修订结果：

- 高都 5 项尺寸转写从 `measured` 降为 `demo`，两个项目都禁止对外正式交付。
- 导入增加深层 schema、长度、数量、ID、框坐标、来源状态和资源白名单；HTML 转义覆盖引号。
- 比例、规范、项目资源、遮挡和尺寸关系从项目数据读取；东呈使用五开间关系。
- AI 请求进行中禁止切换或导入，项目度量按 projectId 记录。
- 正式图预览带版本元数据，业务数据变化后标为 `stale`；检查也会失效。
- 导出包分离原始证据、当前业务、工作流和审计记录。

## 3. 实施文件

### 产品与架构

- `13-8_核心PRD_V1A正式立面成果闭环.md`
- `13-9_V1A技术架构与开发任务.md`
- `13-10_V1A数据基础开发验收记录.md`

### 数据

- `交互原型/02_AI工作台/data/projects/index.json`
- `交互原型/02_AI工作台/data/projects/gaodu.json`
- `交互原型/02_AI工作台/data/projects/dongcheng.json`

### 项目仓库、状态与安全

- `js/data.js`
- `js/store.js`
- `js/main.js`
- `js/api.js`
- `js/ui.js`
- `js/provenance.js`
- `js/metrics.js`
- `server.mjs`

### 项目规则与界面

- `js/datumcheck.js`
- `js/drawgen.js`
- `js/orchestrator.js`
- `js/config.js`
- `js/workspace.js`
- `js/sidebar.js`
- `js/views/start.js`
- `js/views/task.js`
- `js/views/materials.js`
- `js/views/datum.js`
- `js/views/parts.js`
- `js/views/condition.js`
- `js/views/style.js`
- `js/views/drawing.js`
- `js/views/delivery.js`

### 测试与说明

- `server/project-data.test.mjs`
- `package.json`
- `README.md`

## 4. 自动验收

运行：

```powershell
npm run check
npm test
```

结果：语法检查通过；10 项测试全部通过，0 失败。

新增测试覆盖：

- 两个内置数据集通过同一 schema，名称、构件和尺寸关系不同。
- 两个项目都没有伪造 `measured`，并明确禁止对外正式交付。
- 远程资源、越界位置框、未知来源和危险对象字段被拒绝。
- 导出包不含消息和密钥特征，重新导入时生成副本 ID 与 `originProjectId`。
- 高都和东呈 localStorage 状态隔离。
- 旧单键存档保留，复制迁移后不删除旧键。
- AI 请求进行中项目切换被阻止。
- 业务数据变化后既有正式图预览变为 `stale`。
- localStorage 写入失败时提示立即导出。
- HTML 文本与属性引号被转义。

## 5. 浏览器冒烟验收

本地服务：`http://127.0.0.1:8795/`

已验证：

- `/data/projects/index.json` 可由本地静态服务读取。
- 项目总览同时显示高都和东呈，并提供导入、导出入口。
- 切换东呈后显示 18 个构件和 `assets/dongcheng_main.jpg`，没有高都图片残留。
- 东呈尺寸页显示 0 项现场实测，门高 2500mm 只标为照片比例参考。
- 东呈图纸页显示 `assets/dongcheng_elevation.png`，文案为“既有代理成果预览，不随当前数据自动更新”。
- 检查未运行时明确提示“尚未按当前数据运行检查”；运行后得到 2 项自动处理、4 类人工问题，3 项构件存疑继续阻止交付。
- 高都旧存档中的演示 `measured` 已降级，模式栏显示 0 项现场实测；资料 m7 修正为“代理实测记录转写”。
- 浏览器控制台无错误。

## 6. 回退与恢复边界

当前目录不是 Git 仓库，不能提供提交级一键回退。未经 JIAJIA 确认，本轮没有初始化仓库，也没有删除旧浏览器存档。

现有恢复能力：

- 项目业务数据可独立导出和重新导入。
- 旧 `gujian-wt-state` 保留；新状态写入 `gujian-wt-state:<projectId>`。
- 东呈可通过从 `index.json` 移除该索引项独立停用，不影响高都 JSON。
- 导入/导出入口可独立停用，两个内置项目仍能读取。
- 正式图预览与实时校核图分离，任一预览资源可移除而不破坏结构化数据。
- 每个开发任务的文件范围和验收命令已在本文与 `13-9` 记录。

限制：本轮多个任务共同修改了 `store.js`、`data.js` 和部分视图，若要恢复到 2026-08-09 之前的精确代码状态，需要先建立版本管理或使用外部文件历史。本次不声称已经具备该能力。

## 7. 下一决策

由 JIAJIA 决定：

1. 是否在当前目录建立 Git 基线，使后续 D6、D7 可按任务独立提交和回退。
2. 下一步先做 D6 编排器分层，还是 D7 数据驱动正式 DXF。
3. 是否为高都或东呈补齐真实任务书、原始实测记录和责任人，用于真正的 V1A 正式成果验收。

在这些决策之前，不扩展多图种、区域档案或完整平台。
