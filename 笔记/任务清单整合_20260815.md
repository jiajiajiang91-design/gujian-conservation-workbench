# 任务清单整合（2026-08-15 晚）

> 依据：`Codex接手复查与任务清单_20260814.md`（阶段主干）、`代码技术借鉴整合_给执行会话_20260815.md`（O1 至 O9）、`B3执行状态_20260815.md`、执行会话 2026-08-15 实时状态。
> 用途：三条线并行的统一视图。执行会话负责收口线，主对话（本会话）负责文档线，PRD 线等 JIAJIA 启动。

## 1. 当前状态快照（2026-08-15 晚）

- 执行会话：空闲等待中。第三项目图纸求交作业已跑约 7 个 CPU 小时，无同规模完成先例，执行会话已提出整夜跑或中止并行化两案，JIAJIA 默认按整夜跑处理。
- 已完成：阶段 A（资产保全，4 提交）、B1/B2（转换器构件级重写）、B2b（形制修正）、B3 主链（导入、几何生成、图纸 worker 服务端产出均有证据）。
- B3 剩余：图纸入库提交、导出回导闭环、截图证据、e2e 提交（依赖当前作业结果与浏览器面板在场操作）。

## 2. 收口线（执行会话负责，顺序不变）

| 序 | 任务 | 状态与说明 |
|---|---|---|
| 1 | B3 收尾 | 等当前求交作业结果。跑完则接图纸入库、导出回导、截图、e2e；明早未完则中止 |
| 2 | B3.5 图纸 worker 并行化（新增） | 无论今晚结果如何都要做。依据两点：本轮实测第三项目求交单核 7 小时级且无完成先例；B3 验收含清库回导后再生成，作业至少还要跑一遍。按视图拆分多进程属通用性能修复。实施计划经 JIAJIA 确认后执行 |
| 3 | B4（T13 入口切换） | 未开始，B3 闭环后进入 |
| 4 | C 图纸差距清单加 P1 处理 | 未开始。新增素材：本轮求交实际耗时作为性能差距量化证据记入验证材料 |
| 5 | D GitHub 求职展示 | 未开始，多确认点 |
| 6 | 里程碑三综合审查 | 未开始 |

## 3. 动作层线（2026-08-15 JIAJIA 决定提前启动，目标：助手从问答工具变为能操作工作台）

原口径"实现批等 PRD"作废。能力链：表 10 定义（修订稿）→ 动作分发层（网关函数调用加作业接口接线）→ 前端对话衔接。三步完成后模型可触发修改参数（逐条确认生效）、生成三维与图纸、运行检查、导出等 13 个动作，后端作业接口均已存在。

| 序 | 任务 | 负责 | 时点与约束 |
|---|---|---|---|
| D1 | 表 10 修订稿确认 | JIAJIA | 即刻。修订稿即实现规格，后续按稿执行 |
| D2 | 动作目录与校验模块（动作定义数据、模型侧三字段投影、参数校验、结构化错误） | 主对话 | 现在开始。只建新文件不改共享文件，不与收口线冲突 |
| D3 | 分发器（动作路由到三组作业接口与项目操作，失败分级，调用留痕含模型原文） | 主对话起，执行会话接 | 新文件先行，接线部分等 B3 闭环 |
| D4 | 网关函数调用与路由接线（改 kimi-gateway 与 index.ts） | 执行会话 | B3/B4 闭环后第一优先，避免与收口改动撞文件 |
| D5 | 前端对话卡片（动作确认、进度、结果展示，接表 4 消息四类型） | 执行会话 | 随 D4 |
| D6 | 分发测试集（13 动作各至少 3 条真实表达加 5 条边界表达，记录三档命中率） | 主对话准备用例，执行会话跑 | D4 后验收 |

## 4. 文档线（主对话已启动，不占执行会话）

| 条目 | 状态 |
|---|---|
| O9 识别上限证据入 13-2 | 已完成。13-2 表 4 新增点云构件语义分割行，含 mIoU 43.7% 与双来源引用 |
| O3/H1 表 10 修订 | 修订稿已起草（`表10修订稿_待确认_20260815.md`：13 个动作八字段全表、第 3 代占位表、确认机制四规则、软硬分层段），待 JIAJIA 确认后合入 13-6 |
| O2 确认规则入文 | 内容已含在表 10 修订稿第 3 节，随修订稿一起合入 |
| 测试集双维指标建议 | 归入 PRD 线模型测试集任务时引用（几何评尺寸误差、拓扑评接口正确性，参照 BIMNet 方法） |

## 5. PRD 线（等 JIAJIA 启动，不排期）

O4 声明式规则层、O5 受控词表、O6 形制参数层（假设待拍板）、核心 PRD 收束本身。O7（曲线体素）、O8（场景文件）为可选，需求触发。

## 6. 待 JIAJIA 决策（按紧急度排序）

1. 明早图纸作业处置：跑完则续走；未完则中止并批准 B3.5 实施计划。
2. 表 10 修订稿确认：第 2 代 7 个动作是否照单全收、确认级别措辞、第 3 代有无提前项（如解析任务书）。确认后合入 13-6。
3. O6 形制参数加实测覆盖分层：PRD 数据模型章节讨论时拍板。
4. 投递时点已定：图纸作业结束后主对话自动向执行会话投递整合文件（监视已挂）。

## 7. 协作纪律（避免两线冲突）

- 主对话只改文档（13-x、笔记），不碰 apps、workers、服务器、浏览器、作业，不提交 git；改动留在工作区由收口线统一处理。
- 执行会话收到投递后按其中即时要求执行（并任务、核对 1.1 节评估、回执），文档批中已由主对话完成的条目核对而非重做。
