# 古建保护成果生产与持续归档工作台：核心 PRD

> 版本：V1.0  
> 当前实施阶段：V1A 代理交付验证  
> 状态：开发基线  
> 更新日期：2026-08-09  
> 决策依据：`12_调研结论.md`、`13-1` 至 `13-7`、`笔记/辅导10要点笔记.md`、`笔记/辅导11要点笔记.md`、`.claude/pm-context.md`

> 2026-08-09 实施说明：数据基础 D1 至 D5 已完成；高都与东呈均为 `demo-proxy`，不具备可核查原始实测证据，因此本 PRD 定义的 V1A 正式成果退出条件尚未满足。

## 1. 文档目的

本文档把前期调研、问题审计、能力划分、用户旅程和界面方案收束为一个可开发、可验收的产品定义。它不是对 `12`、`13-1` 至 `13-7` 的拼接，也不覆盖作为历史记录保留的 `07_PRD_MVP图纸闭环.md`。

当前只确认 V1A：以一个单体古建筑、一个正式正立面成果为对象，由团队扮演真实使用者和交付责任人，完整走通从任务要求到交付包的闭环。V1A 通过后，才进入真实项目的 V1B。

## 2. 产品定义

### 2.1 一句话定义

古建保护成果生产与持续归档工作台，是面向古建测绘与保护项目执行人员的成果生产工具：把分散的任务要求、照片、草图、实测数据和人工判断组织为可追溯的结构化项目数据，再由确定性规则生成并检查正式成果，最后形成可交付、可复核、可继续更新的项目包。

### 2.2 当前要解决的问题

现有工作的问题不是缺少单个绘图或识别功能，而是证据、对象、判断、图纸和责任之间没有稳定连接：

- 任务要求散落在委托书、聊天和个人理解中，交付口径容易漂移。
- 照片、草图和实测表各自存在，尺寸与构件缺少统一对象编号。
- 识别结果、人工修正和规则计算混在一起，用户无法判断结果从哪里来。
- 图纸发生问题时难以追溯到对应证据和数据，也难以局部重做。
- 交付文件可以生成，但缺少“为什么可以交付、由谁确认”的完整记录。

### 2.3 产品价值

V1A 不以“替代专业人员”为目标，而是验证三项价值：

1. 降低资料整理、重复录入、跨文件核对和返工的成本。
2. 让每项正式结果都能回到原始证据、结构化对象和人工责任节点。
3. 让 AI 处理非标准输入和交互，规则处理确定性结果，人负责专业判断与交付责任。

## 3. 范围与约束

### 3.1 V1A 范围

- 对象：一个单体古建筑。
- 成果：一张正立面现状测绘成果及其交付包。
- 使用者：项目执行人员；产品团队代理扮演项目负责人和委托方确认人。
- 输入：任务书文本或图片、现场照片、尺寸草图、实测数据、必要的历史参考资料。
- 输出：结构化项目数据、构件与现状记录、正立面图、检查记录、人工确认记录、交付清单和可导出的项目数据包。
- 数据集：至少两个对象与构件差异明显的独立项目数据集，用于验证产品不是只适配一个样例。

### 3.2 明确不做

- 不扩展为多图种、多专业协同平台。
- 不建设区域级古建总档案或公共数据门户。
- 不承诺端到端自动生成可交付成果。
- 不让模型直接生成正式图纸或直接通过质量检查。
- 不在 V1A 证明识别准确率、商业规模或长期归档价值。
- 不把示例数据、模型推测或程序生成值标记为现场实测。

### 3.3 阶段退出条件

V1A 只有在以下条件全部满足时才可进入 V1B：

- 团队能在不修改源代码的情况下导入、选择和导出两个不同项目数据集。
- 主流程从任务定义走到交付包，关键责任节点均有人工确认记录。
- 正式成果所用的每项关键数据可追溯到来源；缺失或不确定项不能被静默补齐。
- 规则检查可重复执行，同一份确认数据得到一致结果。
- 无实测基准或存在高影响未决项时，系统明确降级为草图并阻止正式交付。
- 记录完成人工基线、时间结构、返工次数、停靠点数量和交付完整度，形成 V1B 的比较基线。

## 4. 用户与核心场景

### 4.1 主要用户

**项目执行人员（原型角色：李工）**

- 整理任务要求和现场资料。
- 录入或核对实测尺寸、构件、现状和出图设置。
- 处理系统无法负责的专业判断。
- 对最终成果和交付包负责。

**项目负责人或委托方代表（原型角色：王工）**

- 确认交付范围、成果性质和关键变更。
- 接受正式成果或提出退回意见。

### 4.2 核心任务

“李工收到一个单体古建正立面测绘任务。他把任务书、照片、尺寸草图和实测数据放入工作台。系统分类并整理资料，李工只在任务边界、关键构件判断、实测基准、规则无法决定的问题和最终交付处确认。系统据此生成并检查图纸，王工确认后形成可追溯交付包。”

### 4.3 主流程

1. 选择或导入项目。
2. 核对任务要求与正式成果口径。
3. 核对资料完整性、类型和用途。
4. 建立实测基准与尺寸关系。
5. 核对构件身份、位置与证据。
6. 记录现状和病害。
7. 确认出图规则与表达设置。
8. 生成图纸并执行独立规则检查。
9. 处理需要人工决定的问题。
10. 负责人确认并导出交付包与项目数据。

### 4.4 异常流程

- 任务范围不清：停在任务确认，保留已解析内容，不进入正式生产。
- 资料不可读或类型不明：显示失败原因，允许替换、重试或人工归类。
- 缺少实测基准：允许继续整理构件和现状，但成果标记为草图，不能正式交付。
- 构件身份不确定：只挂起受影响对象，其他对象可继续处理。
- 规则冲突：规则只报告冲突，不替用户选择专业结论。
- 甲方退回：记录退回原因和影响范围，只重做受影响阶段与下游结果。
- 模型服务不可用：保留确定性流程和已录入数据，允许人工录入，不伪装为模型已处理。

## 5. 产品原则

1. **先证据，后对象，再成果。** 原始证据、业务对象和正式输出是三类独立数据。
2. **AI 提建议，规则算结果，人承担责任。** 三者在界面和记录中必须可区分。
3. **确定性状态机控制流程。** 模型可以识别意图，但不能自行跳过责任节点。
4. **最少打断。** 只在范围、专业判断、质量责任和交付责任处停靠。
5. **局部更新。** 单项修正只重算受影响对象与下游结果，不清空整个项目。
6. **可恢复。** 项目数据、操作记录和生成结果可导出；开发任务可独立启停与回退。
7. **来源真实。** `demo`、`ai`、`program`、`measured`、`human`、`unknown`、`missing` 不得混用。

## 6. 功能需求

### FR-01 项目数据管理（P0）

**目标**：项目内容与界面代码分离，使同一套产品可以加载不同项目。

**要求**：

- 每个项目使用独立、可读的 JSON 数据包，并带有 schema 版本和项目唯一 ID。
- 项目总览显示可用项目；用户可切换到任一有效项目，不需要修改代码。
- 支持从本地导入 JSON；导入前检查结构、必填字段和 ID 冲突。
- 支持导出当前项目数据，包含原始项目基线、当前人工修正和操作记录。
- 浏览器存档按项目隔离；切换项目不得覆盖另一项目的状态。
- 至少内置“高都玉皇庙主殿”和“东呈古佛堂正殿”两个差异明显的数据集。

**验收**：连续切换两个项目后，名称、资料、构件、图片和状态均对应当前项目；修改 A 项目后切换 B 再返回 A，修改仍存在且 B 未受影响；导出后重新导入可通过校验。

### FR-02 任务要求确认（P0）

**目标**：把任务书和口头要求收束为一份可确认的任务卡。

**要求**：

- 保存任务书原文，不以解析结果覆盖原文。
- 任务卡至少包含对象、成果、范围、比例、规范依据、时间、甲方要求和责任人。
- 显示每个字段的来源与状态；缺失项明确标记。
- AI 可提出解析结果，但用户确认前不得作为正式范围。
- 用户确认后记录确认人、时间和版本。

**验收**：任务原文与任务卡可并列查看；缺少成果范围时流程停靠；人工改动保留修改前值和原因。

### FR-03 资料接收与证据管理（P0）

**目标**：知道收到什么、能否使用、将用于什么判断。

**要求**：

- 资料项至少包含 ID、类型、文件、名称、用途、可用性、说明、来源和状态。
- 同一文件可以服务多个判断，但引用必须记录文件和区域。
- 不可读、重复、无关或缺少关键视角的资料要单独提示。
- 图片框选可形成对对象或问题的证据引用。

**验收**：任一构件或现状记录可打开其证据；删除或替换证据时，系统提示受影响对象。

### FR-04 实测基准与尺寸关系（P0）

**目标**：建立可用于正式出图的尺寸依据。

**要求**：

- 实测记录至少包含 ID、测项、数值、单位、来源、证据、状态和确认信息。
- `measured` 只用于现场实测或经人工确认等同于现场实测的记录。
- 估算、换算和规则推导必须分别标记为 `ai` 或 `program`，不能提升为实测。
- 系统检查必要基准是否存在，以及分尺寸与总尺寸是否冲突。
- 无实测基准时允许生成校核草图，但关闭正式交付。

**验收**：删除全部实测基准后，正式成果状态立即降级；补充并确认基准后可重新计算受影响图纸。

### FR-05 构件核对（P0）

**目标**：建立可追溯的立面对象清单。

**要求**：

- 构件至少包含编号、名称、类别、位置框、尺寸、依据、来源状态和置信等级。
- AI 识别项在人工确认前保持 `ai`；示例预置保持 `demo`。
- 用户可改名、改类、改框、补项和判定不存在；每次操作保留原因。
- 存疑项以问题队列呈现，只阻塞受影响对象。
- 已确认判断可进入项目构件库；误识别可进入排除记录。

**验收**：任一人工修改可追溯到原值、操作者、时间和理由；构件数和框选结果在图像与表格间同步。

### FR-06 现状记录（P0）

**目标**：让现状描述与构件、位置和证据对应。

**要求**：

- 现状记录至少包含 ID、部位、类型、程度、位置、描述、处理方式、证据和状态。
- 通用规则可自动补齐表达格式，专业结论由人确认。
- 现状变化只触发相关标注和检查，不重置其他阶段。

**验收**：从现状记录能定位到构件和图片区域；人工确认后状态与修改历史同步更新。

### FR-07 规则出图（P0）

**目标**：由确定性数据和规则生成可重复的正立面成果。

**要求**：

- 图纸由参数化规则生成，模型不直接输出正式几何结果。
- 图名、对象名、比例、图层、线型、尺寸和说明来自确认数据与出图规则。
- 正式图与数据校核图分开呈现，避免把示意预览当成正式成果。
- 项目资源中必须声明正式图预览和辅助图片；不得在视图代码中固定某一项目文件名。
- 既有正式图预览必须记录来源项目、生成版本、时间、参数摘要和 `preview/current/stale` 状态；数据变化后自动失效。
- 数据变化后只重算受影响部分，并记录生成版本。

**验收**：相同输入重复生成结果一致；切换项目后标题和资源同步变化；缺少必要尺寸时图纸带有不可正式交付标识。

### FR-08 独立质量检查（P0）

**目标**：把能规则化的检查从人工记忆中移出，同时保留人的专业责任。

**要求**：

- 检查与生成分离，至少覆盖必填数据、尺寸闭合、状态来源、未决问题、图纸表达和交付完整度。
- 规则可自动处理的问题、必须人工决定的问题、已解决问题分组显示。
- 每条问题包含类型、对象或位置、说明、处理方式和结论。
- 检查未通过时不得伪造“检查完成”。

**验收**：人工未处理的高影响问题会阻止交付；修改相关数据后重新检查，问题状态按规则变化。

### FR-09 人工责任节点与退回（P0）

**目标**：在必要节点取得明确决定，并可管理返工影响。

**要求**：

- 固定责任节点：任务范围、关键构件判断、实测基准、规则无法决定的问题、最终交付。
- 确认记录包含节点、决定、确认人、时间、依据和影响范围。
- 退回需选择原因和目标阶段；系统保留已完成数据，标记下游结果待更新。
- 系统不得用“继续”替代真正的确认内容。

**验收**：从交付记录能还原所有责任节点；退回构件阶段不会删除任务卡和资料记录。

### FR-10 交付与归档（P0）

**目标**：形成可交接、可复核、可继续更新的交付包。

**要求**：

- 交付清单区分图纸、结构化数据、检查报告、确认记录和说明文件。
- 每个文件包含状态、版本、生成时间和来源。
- 正式交付前检查实测基准、未决问题、规则检查和负责人确认。
- 导出的项目 JSON 是交付包的一部分，能够重新导入工作台。
- 交付完成后仍保留版本和修改记录，不冻结为无法继续更新的文件堆。

**验收**：不满足门槛时只能导出“草图/工作包”；满足门槛后才显示“正式成果”；导出包清单和系统状态一致。

### FR-11 单一 AI 助手与任务路由（P1）

**目标**：用自然语言降低操作成本，但不让对话替代结构化工作。

**要求**：

- 用户只面对一个助手；内部按任务类型路由，不展示多智能体角色。
- 支持资料分类、任务书提取、草图转写、视觉识别、专业问答、意图路由六类模型任务。
- 模型输出采用结构化结果，并经过 schema 校验后才写入项目状态。
- 写入前后显示处理过程、来源和影响；失败时说明失败原因并保留人工入口。
- 对话中的结论必须写入相应业务对象，不能只停留在消息记录。

**验收**：关闭模型服务后主流程仍可人工完成；模型返回无效 JSON 时不污染业务数据。

### FR-12 可追溯状态与操作记录（P1）

**目标**：让任何关键结果都能回答“从哪里来、谁改过、影响什么”。

**要求**：

- 统一使用来源状态枚举，界面显示当前项目的数据来源概况。
- 所有关键写操作记录动作、对象、前后值、操作者、时间、理由和影响。
- 项目状态按项目 ID 独立保存；恢复示例只清空当前项目。
- 导入、导出、切换和 schema 迁移均写入系统记录。

**验收**：随机抽查任务字段、实测项、构件、检查问题和交付文件，均能确认来源或明确显示缺失。

## 7. 最小数据模型

### 7.1 数据层级

| 层级 | 内容 | 是否允许覆盖原始值 |
|---|---|---|
| 原始证据 | 任务书、照片、草图、实测表、历史资料 | 不允许；只能新增版本或标记不可用 |
| 业务对象 | 任务卡、实测项、构件、现状、存疑、出图设置 | 允许通过受控操作修改，必须留历史 |
| 正式输出 | 图纸、检查报告、交付清单、确认记录 | 由确认数据生成；变更时生成新版本 |

### 7.2 项目数据包必填结构

| 字段 | 含义 | 来源 | 必填 | 更新方式 | 主要影响 |
|---|---|---|---|---|---|
| `schemaVersion` | 数据结构版本 | 系统 | 是 | 迁移程序 | 能否导入 |
| `项目.id` | 项目唯一标识 | 建项人/系统 | 是 | 建项后不可直接改 | 存档隔离、引用 |
| `项目.名称` | 正式对象名称 | 任务书/人工 | 是 | 人工确认 | 标题栏、图签、交付 |
| `项目.成果` | 目标成果 | 任务书/人工 | 是 | 人工确认 | 流程与交付门槛 |
| `资源` | 主图、斜视图、正式图预览 | 资料/系统 | 是 | 替换并记录 | 证据视图、出图预览 |
| `任务书原文` | 未改写的任务依据 | 原始证据 | 是 | 新增版本 | 任务解析与复核 |
| `任务卡` | 已结构化的任务范围 | AI 建议+人工 | 是 | 受控修改 | 全流程 |
| `资料[]` | 输入文件清单 | 上传/导入 | 是 | 增删并记录 | 证据可用性 |
| `实测[]` | 控制尺寸与测项 | 现场/人工 | 是，可为空 | 受控修改 | 正式出图资格 |
| `构件[]` | 立面业务对象 | AI/人工 | 是 | 受控修改 | 图纸、现状、检查 |
| `存疑[]` | 未决专业问题 | AI/规则/人工 | 是，可为空 | 人工解决 | 局部阻塞、交付 |
| `现状[]` | 现状与病害 | AI/人工 | 是，可为空 | 受控修改 | 图纸标注、说明 |
| `图纸样式` | 表达规则 | 规范/人工 | 是 | 人工确认 | 生成与检查 |
| `检查问题[]` | 质量检查结果 | 规则/人工 | 是，可为空 | 重新运行检查 | 交付门槛 |
| `交付` | 文件和确认信息 | 系统/人工 | 是 | 生成新版本 | 正式交付 |

### 7.3 来源状态

| 状态 | 定义 | 可否直接作为正式依据 |
|---|---|---|
| `measured` | 现场实测或经责任人确认的实测记录 | 可以 |
| `human` | 人工专业判断或确认 | 视字段责任要求而定 |
| `program` | 确定性程序或规则计算 | 可以，但必须可追溯到输入 |
| `ai` | 模型识别、提取或建议 | 不可以，需按节点确认 |
| `demo` | 演示预置数据 | 不可以 |
| `unknown` | 已知存在但结论不确定 | 不可以 |
| `missing` | 必要数据缺失 | 不可以 |

## 8. 状态与权限

### 8.1 流程状态

- `idle`：未开始。
- `running`：系统或用户正在处理。
- `stop`：到达责任节点，等待明确决定。
- `done`：该阶段当前版本已完成。
- `stale`：上游变化，当前结果需要更新。
- `blocked`：必要数据或服务缺失，无法继续该分支。

### 8.2 交付状态

- 工作中。
- 草图可导出，不可正式交付。
- 待人工确认。
- 正式成果可交付。
- 已交付。
- 已退回。

### 8.3 权限原则

V1A 不建设复杂权限系统，但确认记录必须区分执行人员与负责人。只有负责人身份可以完成最终交付确认；示例代理操作必须显式标记为代理验证，不得冒充真实甲方。

## 9. 非功能需求

- **真实性**：界面任何时候都能看出当前结果是示例、AI、程序、实测还是人工形成。
- **一致性**：相同确认数据和规则版本得到相同检查与图纸结果。
- **可恢复性**：单项目数据可独立导出、导入；无效导入不覆盖当前项目。
- **可维护性**：数据、状态、业务规则、模型调用和视图分层；禁止在视图中继续堆叠项目特例。
- **性能**：两个内置项目切换后应在一次页面交互内完成渲染；模型调用不得阻塞已存在数据的浏览与人工编辑。
- **安全与隐私**：文件默认本地处理；调用外部模型前明确告知传输范围；密钥只在服务端环境变量中，不进入项目 JSON、日志和前端代码。
- **兼容性**：当前支持现代桌面浏览器；V1A 不承诺移动端完整生产能力。

## 10. 验证与指标

V1A 先建立基线，不虚构提升比例。每次代理交付记录：

- 总完成时间、各阶段时间和等待时间。
- 人工有效操作次数、责任节点数量和无效打断数量。
- AI 建议直接采用、修改后采用、拒绝的数量。
- 规则自动发现和处理的问题数量。
- 返工次数、返工起点和受影响范围。
- 关键字段来源完整度、未决问题数、交付文件完整度。
- 同一数据重复生成的一致性。
- 第二数据集无需改代码即可完成的流程比例与失败点。

V1B 才以真实人工流程作为对照，判断是否减少时间和重复劳动。没有真实基线前，不设“效率提升 50%”等宣传性目标。

## 11. V1A 验收场景

### 场景 A：高都玉皇庙主殿完整代理流程

1. 选择高都项目，核对任务与资料。
2. 处理构件存疑和现状记录。
3. 按实测与规则生成、检查图纸。
4. 完成负责人代理确认，但不冒充真实责任签发。
5. 导出结构化工作包和项目 JSON。

预期：流程、来源、责任、检查和交付记录完整；既有成果预览与实时校核图用途清楚；由于原始实测证据未进入原型，只能形成代理验证结果，正式交付保持关闭。

### 场景 B：东呈古佛堂差异与降级验证

1. 选择东呈项目，不修改源代码。
2. 显示不同构件系统、图片和任务数据。
3. 因缺少现场实测基准，系统允许继续核对，但成果保持草图。
4. 导出工作包，不允许标为正式成果。

预期：项目差异不依赖页面特例；缺失数据不会被演示值掩盖。

### 场景 C：项目数据往返

1. 在项目中修改一个构件并记录理由。
2. 导出项目 JSON。
3. 以新 ID 导入该 JSON 并打开。

预期：结构校验通过，修改和来源保留；无效文件被拒绝且当前项目不受影响。

### 场景 D：服务不可用

1. 关闭模型服务。
2. 浏览项目、人工修改构件、运行规则检查并导出项目数据。

预期：模型能力明确不可用；确定性功能继续运行；系统不显示虚假模型结果。

## 12. 风险与待验证问题

- 当前正式图仍主要来自既有参数化脚本结果，尚未证明所有业务对象都能驱动正式 CAD 几何。
- 东呈数据来自照片分析与演示估算，不具备正式测绘效力，只用于验证数据结构和降级逻辑。
- V1A 使用代理负责人，不能证明真实委托方的接受度。
- 当前来源状态粒度仍以记录级为主，后续可能需要下沉到字段级。
- 图纸生成与检查的规则覆盖率尚无真实项目基线，需要在 V1B 记录漏检和误报。
- 长期档案更新、跨项目构件知识复用和组织权限不在本阶段验证。

## 13. 后续决策门

完成 V1A 验收后，由 JIAJIA 决定是否进入 V1B。决策材料必须包括：代理交付记录、双数据集结果、未通过项、技术债、真实项目接入成本和需要补充的专业审查。未经确认，不自动扩大到多图种、平台化或长期区域归档。
